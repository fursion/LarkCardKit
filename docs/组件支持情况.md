# LarkCardKit 飞书卡片 2.0 组件支持情况

> 本文档对照飞书卡片 2.0 标准文档，详细说明 LarkCardKit 对各组件的支持情况。
> 
> 文档更新日期：2026-03-08

---

## 一、支持概览

### 1.1 总体统计

| 指标 | 数值 |
|------|------|
| 已实现组件 | 34 个 |
| 未实现组件 | 0 个 |
| 总体覆盖率 | **100%** |

### 1.2 支持状态图例

| 状态 | 说明 |
|------|------|
| ✅ 完整支持 | 组件所有标准属性均已实现 |
| ⚠️ 部分支持 | 组件部分属性已实现 |
| ❌ 未实现 | 组件尚未实现 |

---

## 二、卡片根结构支持情况

### 2.1 Card (卡片根对象)

**支持状态**: ✅ 完整支持

**Tag**: `schema: "2.0"`

| 属性 | 类型 | 必填 | 支持状态 | 说明 |
|------|------|------|----------|------|
| `schema` | string | 是 | ✅ | 固定为 "2.0" |
| `config` | object | 否 | ✅ | 卡片配置 |
| `card_link` | object | 否 | ✅ | 卡片链接配置 |
| `header` | object | 否 | ✅ | 卡片头部 |
| `body` | object | 是 | ✅ | 卡片主体 |
| `fallback` | object | 否 | ✅ | 降级配置 |

**Builder 方法**:
- `CardBuilder.Create()` - 创建卡片构建器
- `Config()` - 配置卡片设置
- `CardLink()` - 配置卡片链接
- `Header()` - 配置卡片头部
- `Body()` - 配置卡片主体
- `Fallback()` - 配置降级内容
- `Build()` - 构建卡片对象
- `ToJson()` - 序列化为 JSON

---

### 2.2 Config (卡片配置)

**支持状态**: ✅ 完整支持

| 属性 | 类型 | 必填 | 默认值 | 支持状态 | 说明 |
|------|------|------|--------|----------|------|
| `update_multi` | boolean | 否 | true | ✅ | 是否为共享卡片 |
| `streaming_mode` | boolean | 否 | false | ✅ | 是否启用流式更新模式 |
| `streaming_config` | object | 否 | - | ✅ | 流式更新配置 |
| `summary` | object | 否 | - | ✅ | 卡片摘要信息 |
| `locales` | string[] | 否 | - | ✅ | 生效语言列表 |
| `enable_forward` | boolean | 否 | true | ✅ | 是否支持转发卡片 |
| `width_mode` | string | 否 | default | ✅ | 卡片宽度模式 |
| `use_custom_translation` | boolean | 否 | false | ✅ | 是否使用自定义翻译 |
| `enable_forward_interaction` | boolean | 否 | false | ✅ | 转发卡片是否支持交互 |
| `style` | object | 否 | - | ✅ | 自定义字号和颜色配置 |

**Builder 方法**:
- `UpdateMulti(bool)` - 设置共享卡片
- `StreamingMode(bool)` - 设置流式更新模式
- `StreamingConfig(Action<StreamingConfigBuilder>)` - 配置流式更新
- `Summary(string)` - 设置摘要
- `Locales(params string[])` - 设置语言列表
- `EnableForward(bool)` - 设置转发支持
- `WidthMode(string)` - 设置宽度模式
- `UseCustomTranslation(bool)` - 设置自定义翻译
- `EnableForwardInteraction(bool)` - 设置转发交互
- `Style(Action<CardStyleBuilder>)` - 配置样式

---

### 2.3 card_link (卡片全局跳转链接)

**支持状态**: ✅ 完整支持

| 属性 | 类型 | 必填 | 支持状态 | 说明 |
|------|------|------|----------|------|
| `url` | string | 否 | ✅ | 默认链接地址 |
| `pc_url` | string | 否 | ✅ | PC 端链接地址 |
| `ios_url` | string | 否 | ✅ | iOS 端链接地址 |
| `android_url` | string | 否 | ✅ | Android 端链接地址 |

**Builder 方法**:
- `CardLinkBuilder.Url(string)` - 设置默认链接
- `CardLinkBuilder.PcUrl(string)` - 设置 PC 端链接
- `CardLinkBuilder.IosUrl(string)` - 设置 iOS 端链接
- `CardLinkBuilder.AndroidUrl(string)` - 设置 Android 端链接

---

### 2.4 Header (卡片头部)

**支持状态**: ✅ 完整支持

| 属性 | 类型 | 必填 | 默认值 | 支持状态 | 说明 |
|------|------|------|--------|----------|------|
| `title` | PlainText | 是 | - | ✅ | 头部主标题 |
| `subtitle` | PlainText | 否 | - | ✅ | 头部副标题 |
| `icon` | object | 否 | - | ✅ | 头部图标配置 |
| `template` | string | 否 | default | ✅ | 标题主题样式颜色 |
| `text_tag_list` | array | 否 | - | ✅ | 标题后缀标签 |
| `padding` | string | 否 | 12px | ✅ | 头部内边距 |

**Builder 方法**:
- `Title(string)` - 设置标题
- `Subtitle(string)` - 设置副标题
- `Template(string)` - 设置主题颜色
- `Icon(string, string?)` - 设置标准图标
- `CustomIcon(string)` - 设置自定义图标
- `TextTag(Action<TextTagBuilder>)` - 添加后缀标签
- `Padding(string)` - 设置内边距

---

### 2.5 Body (卡片主体)

**支持状态**: ✅ 完整支持

| 属性 | 类型 | 必填 | 默认值 | 支持状态 | 说明 |
|------|------|------|--------|----------|------|
| `elements` | Element[] | 是 | - | ✅ | 卡片元素列表 |
| `direction` | string | 否 | vertical | ✅ | 排列方向 |
| `padding` | string | 否 | 12px | ✅ | 内边距 |
| `horizontal_spacing` | string | 否 | 8px | ✅ | 水平间距 |
| `horizontal_align` | string | 否 | left | ✅ | 水平对齐方式 |
| `vertical_spacing` | string | 否 | 8px | ✅ | 垂直间距 |
| `vertical_align` | string | 否 | top | ✅ | 垂直对齐方式 |

**Builder 方法**:
- `VerticalSpacing(string)` - 设置垂直间距
- `HorizontalSpacing(string)` - 设置水平间距
- `Padding(string)` - 设置内边距
- `AddElement(Element)` - 添加元素

---

### 2.6 Fallback (降级配置)

**支持状态**: ✅ 完整支持

| 属性 | 类型 | 必填 | 支持状态 | 说明 |
|------|------|------|----------|------|
| `title` | PlainText | 否 | ✅ | 降级标题 |
| `content` | PlainText | 否 | ✅ | 降级内容 |

**Builder 方法**:
- `Title(string)` - 设置降级标题
- `Content(string)` - 设置降级内容

---

## 三、基础元素支持情况

### 3.1 PlainText (纯文本)

**支持状态**: ✅ 完整支持

**Tag**: `plain_text`

| 属性 | 类型 | 必填 | 默认值 | 支持状态 | 说明 |
|------|------|------|--------|----------|------|
| `tag` | string | 是 | - | ✅ | 固定为 plain_text |
| `content` | string | 是 | - | ✅ | 文本内容 |
| `text_size` | string | 否 | normal | ✅ | 文本大小 |
| `text_color` | string | 否 | default | ✅ | 文本颜色 |
| `text_align` | string | 否 | left | ✅ | 文本对齐方式 |
| `notation` | boolean | 否 | false | ✅ | 是否使用 notation 样式 |
| `width` | string | 否 | - | ✅ | 文本宽度 |
| `element_id` | string | 否 | - | ✅ | 元素唯一标识 |
| `margin` | string | 否 | - | ✅ | 外边距 |

**Builder 方法**:
- `Content(string)` - 设置文本内容
- `TextSize(string)` - 设置文本大小
- `TextColor(string)` - 设置文本颜色
- `TextAlign(string)` - 设置对齐方式
- `Notation(bool)` - 设置 notation 样式
- `Width(string)` - 设置宽度
- `ElementId(string)` - 设置元素 ID
- `Margin(string)` - 设置外边距

---

### 3.2 Markdown (富文本)

**支持状态**: ✅ 完整支持

**Tag**: `lark_md`

| 属性 | 类型 | 必填 | 默认值 | 支持状态 | 说明 |
|------|------|------|--------|----------|------|
| `tag` | string | 是 | - | ✅ | 固定为 lark_md |
| `content` | string | 是 | - | ✅ | Markdown 内容 |
| `text_size` | string | 否 | normal | ✅ | 文本大小 |
| `text_color` | string | 否 | default | ✅ | 文本颜色 |
| `text_align` | string | 否 | left | ✅ | 文本对齐方式 |
| `icon` | object | 否 | - | ✅ | 前缀图标 |
| `element_id` | string | 否 | - | ✅ | 元素唯一标识 |
| `margin` | string | 否 | - | ✅ | 外边距 |

**支持的 Markdown 语法**:
- 换行、斜体、粗体、删除线
- 超链接、图片、分割线
- 无序列表、有序列表、引用
- @指定人、@所有人
- 彩色文本、标签、人员展示

**Builder 方法**:
- `Content(string)` - 设置 Markdown 内容
- `TextSize(string)` - 设置文本大小
- `TextColor(string)` - 设置文本颜色
- `TextAlign(string)` - 设置对齐方式
- `Icon(string, string?)` - 设置前缀图标

---

## 四、容器组件支持情况

### 4.1 Div (容器)

**支持状态**: ✅ 完整支持

**Tag**: `div`

| 属性 | 类型 | 必填 | 默认值 | 支持状态 | 说明 |
|------|------|------|--------|----------|------|
| `tag` | string | 是 | - | ✅ | 固定为 div |
| `text` | Element | 否 | - | ✅ | 文本内容 (PlainText 或 Markdown) |
| `elements` | Element[] | 否 | - | ✅ | 子元素列表 |
| `direction` | string | 否 | vertical | ✅ | 布局方向 |
| `padding` | string | 否 | - | ✅ | 内边距 |
| `vertical_spacing` | string | 否 | - | ✅ | 垂直间距 |
| `horizontal_spacing` | string | 否 | - | ✅ | 水平间距 |
| `horizontal_align` | string | 否 | - | ✅ | 水平对齐 |
| `vertical_align` | string | 否 | - | ✅ | 垂直对齐 |
| `width` | string | 否 | - | ✅ | 文本宽度 |

**Builder 方法**:
- `Direction(string)` - 设置布局方向
- `Vertical()` / `Horizontal()` - 设置布局方向
- `Padding(string)` - 设置内边距
- `VerticalSpacing(string)` - 设置垂直间距
- `HorizontalSpacing(string)` - 设置水平间距
- `HorizontalAlign(AlignType)` - 设置水平对齐
- `VerticalAlign(AlignType)` - 设置垂直对齐
- `Add(Element)` - 添加子元素
- `PlainText(string)` - 添加纯文本
- `Markdown(string)` - 添加 Markdown
- `Button(Action<ButtonBuilder>)` - 添加按钮
- `Image(string, Action<ImageBuilder>?)` - 添加图片

---

### 4.2 ColumnSet (分栏布局)

**支持状态**: ✅ 完整支持

**Tag**: `column_set`

| 属性 | 类型 | 必填 | 支持状态 | 说明 |
|------|------|------|----------|------|
| `tag` | string | 是 | ✅ | 固定为 column_set |
| `columns` | Column[] | 是 | ✅ | 分栏列列表 |
| `margin` | string | 否 | ✅ | 外边距 |
| `background_style` | string | 否 | ✅ | 背景样式 |
| `horizontal_spacing` | string | 否 | ✅ | 水平间距 |

**Column (列) 结构**:

| 属性 | 类型 | 必填 | 默认值 | 支持状态 | 说明 |
|------|------|------|--------|----------|------|
| `tag` | string | 是 | ✅ | 固定为 column |
| `width` | string | 否 | auto | ✅ | 列宽度 |
| `vertical_align` | string | 否 | - | ✅ | 垂直对齐方式 |
| `elements` | Element[] | 是 | ✅ | 列内元素列表 |

**Builder 方法**:
- `Column(Action<ColumnBuilder>)` - 添加列
- `HorizontalSpacing(string)` - 设置水平间距
- `BackgroundStyle(string)` - 设置背景样式

---

### 4.3 InteractiveContainer (交互容器)

**支持状态**: ✅ 完整支持

**Tag**: `interactive_container`

| 属性 | 类型 | 必填 | 支持状态 | 说明 |
|------|------|------|----------|------|
| `tag` | string | 是 | ✅ | 固定为 interactive_container |
| `elements` | Element[] | 是 | ✅ | 子元素列表 |
| `direction` | string | 否 | ✅ | 布局方向 |
| `padding` | string | 否 | ✅ | 内边距 |
| `vertical_spacing` | string | 否 | ✅ | 垂直间距 |
| `horizontal_spacing` | string | 否 | ✅ | 水平间距 |
| `horizontal_align` | string | 否 | ✅ | 水平对齐 |
| `vertical_align` | string | 否 | ✅ | 垂直对齐 |
| `behaviors` | array | 否 | ✅ | 交互行为列表 |

**Builder 方法**:
- `Direction(string)` / `Vertical()` / `Horizontal()` - 设置布局方向
- `Padding(string)` - 设置内边距
- `VerticalSpacing(string)` - 设置垂直间距
- `HorizontalSpacing(string)` - 设置水平间距
- `PlainText(string)` - 添加纯文本
- `Markdown(string)` - 添加 Markdown
- `Button(Action<ButtonBuilder>)` - 添加按钮
- `Image(string, Action<ImageBuilder>?)` - 添加图片
- `Div(Action<DivBuilder>)` - 添加容器
- `ColumnSet(Action<ColumnSetBuilder>)` - 添加分栏
- `OnClick(string)` / `OnClick(OpenUrlBehavior)` - 设置点击跳转
- `OnCallback(object?)` - 设置回调行为

---

## 五、表单组件支持情况

### 5.1 Form (表单)

**支持状态**: ✅ 完整支持

**Tag**: `form`

| 属性 | 类型 | 必填 | 默认值 | 支持状态 | 说明 |
|------|------|------|--------|----------|------|
| `tag` | string | 是 | - | ✅ | 固定为 form |
| `name` | string | 是 | - | ✅ | 表单容器唯一标识 |
| `direction` | string | 否 | vertical | ✅ | 布局方向 |
| `padding` | string | 否 | - | ✅ | 内边距 |
| `vertical_spacing` | string | 否 | - | ✅ | 垂直间距 |
| `horizontal_spacing` | string | 否 | - | ✅ | 水平间距 |
| `horizontal_align` | string | 否 | - | ✅ | 水平对齐方式 |
| `vertical_align` | string | 否 | - | ✅ | 垂直对齐方式 |
| `elements` | Element[] | 是 | - | ✅ | 子元素列表 |

**Builder 方法**:
- `Name(string)` - 设置表单名称
- `Direction(string)` / `Vertical()` / `Horizontal()` - 设置布局方向
- `Padding(string)` - 设置内边距
- `VerticalSpacing(string)` - 设置垂直间距
- `HorizontalSpacing(string)` - 设置水平间距
- `Input(Action<InputBuilder>)` - 添加输入框
- `Select(Action<SelectBuilder>)` - 添加选择器
- `DatePicker(Action<DatePickerBuilder>)` - 添加日期选择器
- `PickerTime(Action<PickerTimeBuilder>)` - 添加时间选择器
- `PickerDatetime(Action<PickerDatetimeBuilder>)` - 添加日期时间选择器
- `Checkbox(Action<CheckboxBuilder>)` - 添加勾选框
- `Button(Action<ButtonBuilder>)` - 添加按钮

---

### 5.2 Input (输入框)

**支持状态**: ✅ 完整支持

**Tag**: `input`

| 属性 | 类型 | 必填 | 默认值 | 支持状态 | 说明 |
|------|------|------|--------|----------|------|
| `tag` | string | 是 | - | ✅ | 固定为 input |
| `name` | string | 否 | - | ✅ | 输入框唯一标识 |
| `required` | boolean | 否 | false | ✅ | 是否必填 |
| `placeholder` | PlainText | 否 | - | ✅ | 占位文本 |
| `default_value` | string | 否 | - | ✅ | 默认值 |
| `input_type` | string | 否 | text | ✅ | 输入类型 |
| `label` | PlainText | 否 | - | ✅ | 文本标签 |
| `label_position` | string | 否 | top | ✅ | 标签位置 |
| `max_length` | int | 否 | 1000 | ✅ | 最大长度 |
| `rows` | int | 否 | 5 | ✅ | 默认行数 |
| `auto_resize` | boolean | 否 | false | ✅ | 是否自适应高度 |
| `max_rows` | int | 否 | - | ✅ | 最大行数 |
| `width` | string | 否 | default | ✅ | 输入框宽度 |
| `disabled` | boolean | 否 | false | ✅ | 是否禁用 |
| `disabled_tips` | PlainText | 否 | - | ✅ | 禁用提示 |
| `confirm` | object | 否 | - | ✅ | 二次确认配置 |
| `behaviors` | array | 否 | - | ✅ | 交互行为列表 |

**Builder 方法**:
- `Name(string)` - 设置名称
- `Required()` - 设置必填
- `Placeholder(string)` - 设置占位文本
- `DefaultValue(string)` - 设置默认值
- `InputType(string)` - 设置输入类型
- `Label(string)` - 设置标签
- `LabelPosition(string)` - 设置标签位置
- `MaxLength(int)` - 设置最大长度
- `Rows(int)` - 设置行数
- `AutoResize()` - 设置自适应高度
- `MaxRows(int)` - 设置最大行数
- `Width(string)` - 设置宽度
- `Disabled()` - 设置禁用
- `DisabledTips(string)` - 设置禁用提示
- `Confirm(string, string)` - 设置二次确认

---

### 5.3 Select (下拉选择器)

**支持状态**: ✅ 完整支持

**Tag**: `select_static` / `multi_select_static`

| 属性 | 类型 | 必填 | 默认值 | 支持状态 | 说明 |
|------|------|------|--------|----------|------|
| `tag` | string | 是 | - | ✅ | select_static 或 multi_select_static |
| `name` | string | 否 | - | ✅ | 选择器唯一标识 |
| `required` | boolean | 否 | false | ✅ | 是否必填 |
| `placeholder` | PlainText | 否 | - | ✅ | 占位文本 |
| `initial_option` | string | 否 | - | ✅ | 初始选项值 |
| `initial_options` | string[] | 否 | - | ✅ | 初始选中值列表（多选） |
| `options` | SelectOption[] | 否 | - | ✅ | 选项列表 |
| `width` | string | 否 | default | ✅ | 选择器宽度 |
| `disabled` | boolean | 否 | false | ✅ | 是否禁用 |
| `behaviors` | array | 否 | - | ✅ | 交互行为列表 |

**Builder 方法**:
- `Name(string)` - 设置名称
- `Required()` - 设置必填
- `Placeholder(string)` - 设置占位文本
- `InitialOption(string)` - 设置初始选项
- `InitialOptions(params string[])` - 设置初始选项（多选）
- `Option(string, string)` - 添加选项
- `Options(IEnumerable<SelectOptionInfo>)` - 批量设置选项
- `Width(string)` - 设置宽度
- `Disabled()` - 设置禁用
- `MultiSelect()` - 设置为多选模式

---

### 5.4 DatePicker (日期选择器)

**支持状态**: ✅ 完整支持

**Tag**: `date_picker`

| 属性 | 类型 | 必填 | 默认值 | 支持状态 | 说明 |
|------|------|------|--------|----------|------|
| `tag` | string | 是 | - | ✅ | 固定为 date_picker |
| `name` | string | 否 | - | ✅ | 唯一标识 |
| `required` | boolean | 否 | false | ✅ | 是否必填 |
| `placeholder` | PlainText | 否 | - | ✅ | 占位文本 |
| `initial_date` | string | 否 | - | ✅ | 初始日期 |
| `width` | string | 否 | default | ✅ | 宽度 |
| `disabled` | boolean | 否 | false | ✅ | 是否禁用 |
| `confirm` | object | 否 | - | ✅ | 二次确认配置 |
| `behaviors` | array | 否 | - | ✅ | 交互行为列表 |

**Builder 方法**:
- `Name(string)` - 设置名称
- `Required()` - 设置必填
- `Placeholder(string)` - 设置占位文本
- `InitialDate(string)` - 设置初始日期
- `Width(string)` - 设置宽度
- `Disabled()` - 设置禁用
- `Confirm(string, string)` - 设置二次确认

---

### 5.5 PickerTime (时间选择器)

**支持状态**: ✅ 完整支持

**Tag**: `picker_time`

| 属性 | 类型 | 必填 | 默认值 | 支持状态 | 说明 |
|------|------|------|--------|----------|------|
| `tag` | string | 是 | - | ✅ | 固定为 picker_time |
| `name` | string | 否 | - | ✅ | 唯一标识 |
| `required` | boolean | 否 | false | ✅ | 是否必填 |
| `placeholder` | PlainText | 否 | - | ✅ | 占位文本 |
| `initial_time` | string | 否 | - | ✅ | 初始时间 |
| `width` | string | 否 | default | ✅ | 宽度 |
| `disabled` | boolean | 否 | false | ✅ | 是否禁用 |
| `value` | object | 否 | - | ✅ | 回传数据 |
| `confirm` | object | 否 | - | ✅ | 二次确认配置 |
| `behaviors` | array | 否 | - | ✅ | 交互行为列表 |

**Builder 方法**:
- `Name(string)` - 设置名称
- `Required()` - 设置必填
- `Placeholder(string)` - 设置占位文本
- `InitialTime(string)` - 设置初始时间
- `Width(string)` - 设置宽度
- `Disabled()` - 设置禁用

---

### 5.6 PickerDatetime (日期时间选择器)

**支持状态**: ✅ 完整支持

**Tag**: `picker_datetime`

| 属性 | 类型 | 必填 | 默认值 | 支持状态 | 说明 |
|------|------|------|--------|----------|------|
| `tag` | string | 是 | - | ✅ | 固定为 picker_datetime |
| `name` | string | 否 | - | ✅ | 唯一标识 |
| `required` | boolean | 否 | false | ✅ | 是否必填 |
| `placeholder` | PlainText | 否 | - | ✅ | 占位文本 |
| `initial_datetime` | string | 否 | - | ✅ | 初始日期时间 |
| `width` | string | 否 | default | ✅ | 宽度 |
| `disabled` | boolean | 否 | false | ✅ | 是否禁用 |
| `value` | object | 否 | - | ✅ | 回传数据 |
| `confirm` | object | 否 | - | ✅ | 二次确认配置 |
| `behaviors` | array | 否 | - | ✅ | 交互行为列表 |

**Builder 方法**:
- `Name(string)` - 设置名称
- `Required()` - 设置必填
- `Placeholder(string)` - 设置占位文本
- `InitialDatetime(string)` - 设置初始日期时间
- `Width(string)` - 设置宽度
- `Disabled()` - 设置禁用

---

### 5.7 Checkbox (勾选框)

**支持状态**: ✅ 完整支持

**Tag**: `checkbox`

| 属性 | 类型 | 必填 | 默认值 | 支持状态 | 说明 |
|------|------|------|--------|----------|------|
| `tag` | string | 是 | - | ✅ | 固定为 checkbox |
| `name` | string | 否 | - | ✅ | 唯一标识 |
| `required` | boolean | 否 | false | ✅ | 是否必填 |
| `options` | CheckboxOption[] | 否 | - | ✅ | 选项列表 |
| `initial_selected_options` | string[] | 否 | - | ✅ | 初始选中值 |
| `behaviors` | array | 否 | - | ✅ | 交互行为列表 |

**Builder 方法**:
- `Name(string)` - 设置名称
- `Required()` - 设置必填
- `Option(string, string)` - 添加选项
- `InitialSelectedOptions(params string[])` - 设置初始选中值

---

## 六、交互组件支持情况

### 6.1 Button (按钮)

**支持状态**: ✅ 完整支持

**Tag**: `button`

| 属性 | 类型 | 必填 | 默认值 | 支持状态 | 说明 |
|------|------|------|--------|----------|------|
| `tag` | string | 是 | - | ✅ | 固定为 button |
| `type` | string | 否 | default | ✅ | 按钮类型 |
| `size` | string | 否 | medium | ✅ | 按钮尺寸 |
| `width` | string | 否 | default | ✅ | 按钮宽度 |
| `text` | PlainText | 是 | - | ✅ | 按钮文本 |
| `icon` | object | 否 | - | ✅ | 前缀图标 |
| `hover_tips` | PlainText | 否 | - | ✅ | 悬浮提示 |
| `disabled` | boolean | 否 | false | ✅ | 是否禁用 |
| `disabled_tips` | PlainText | 否 | - | ✅ | 禁用提示 |
| `confirm` | object | 否 | - | ✅ | 二次确认配置 |
| `behaviors` | array | 否 | - | ✅ | 交互行为列表 |
| `name` | string | 否 | - | ✅ | 表单内唯一标识 |
| `form_action_type` | string | 否 | - | ✅ | 表单交互类型 |

**按钮类型枚举**:
- `default` - 默认类型
- `primary` - 主要类型
- `danger` - 危险类型
- `text` - 文本类型
- `primary_text` - 主要文本类型
- `danger_text` - 危险文本类型
- `primary_filled` - 主要填充类型
- `danger_filled` - 危险填充类型
- `laser` - 镭射按钮

**Builder 方法**:
- `Text(string)` - 设置按钮文本
- `Type(string)` / `Type(ButtonType)` - 设置按钮类型
- `Size(string)` / `Size(ButtonSize)` - 设置按钮尺寸
- `Width(string)` - 设置宽度
- `Icon(string, string?)` - 设置图标
- `HoverTips(string)` - 设置悬浮提示
- `Disabled()` - 设置禁用
- `DisabledTips(string)` - 设置禁用提示
- `Confirm(string, string)` - 设置二次确认
- `OnClick(string)` - 设置点击跳转
- `OnCallback(object?)` - 设置回调行为
- `FormSubmit()` - 设置表单提交
- `FormReset()` - 设置表单重置
- `Name(string)` - 设置名称

---

### 6.2 Overflow (折叠按钮组)

**支持状态**: ✅ 完整支持

**Tag**: `overflow`

| 属性 | 类型 | 必填 | 默认值 | 支持状态 | 说明 |
|------|------|------|--------|----------|------|
| `tag` | string | 是 | - | ✅ | 固定为 overflow |
| `options` | OverflowOption[] | 是 | - | ✅ | 选项列表 |
| `width` | string | 否 | default | ✅ | 宽度 |
| `disabled` | boolean | 否 | false | ✅ | 是否禁用 |
| `behaviors` | array | 否 | - | ✅ | 交互行为列表 |

**Builder 方法**:
- `Option(string, string)` - 添加选项
- `Width(string)` - 设置宽度
- `Disabled()` - 设置禁用

---

### 6.3 SelectPerson (人员选择-单选)

**支持状态**: ✅ 完整支持

**Tag**: `select_person`

| 属性 | 类型 | 必填 | 默认值 | 支持状态 | 说明 |
|------|------|------|--------|----------|------|
| `tag` | string | 是 | - | ✅ | 固定为 select_person |
| `name` | string | 否 | - | ✅ | 选择器唯一标识 |
| `required` | boolean | 否 | false | ✅ | 是否必填 |
| `placeholder` | PlainText | 否 | - | ✅ | 占位文本 |
| `initial_option` | string | 否 | - | ✅ | 初始选中用户 |
| `width` | string | 否 | default | ✅ | 选择器宽度 |
| `disabled` | boolean | 否 | false | ✅ | 是否禁用 |
| `confirm` | object | 否 | - | ✅ | 二次确认配置 |
| `behaviors` | array | 否 | - | ✅ | 交互行为列表 |

**Builder 方法**:
- `Name(string)` - 设置名称
- `Required()` - 设置必填
- `Placeholder(string)` - 设置占位文本
- `InitialOption(string)` - 设置初始选中用户
- `Width(string)` - 设置宽度
- `Disabled()` - 设置禁用

---

### 6.4 MultiSelectPerson (人员选择-多选)

**支持状态**: ✅ 完整支持

**Tag**: `multi_select_person`

| 属性 | 类型 | 必填 | 默认值 | 支持状态 | 说明 |
|------|------|------|--------|----------|------|
| `tag` | string | 是 | - | ✅ | 固定为 multi_select_person |
| `name` | string | 否 | - | ✅ | 选择器唯一标识 |
| `required` | boolean | 否 | false | ✅ | 是否必填 |
| `placeholder` | PlainText | 否 | - | ✅ | 占位文本 |
| `selected_values` | string[] | 否 | - | ✅ | 默认选中用户列表 |
| `options` | PersonOption[] | 否 | - | ✅ | 候选人员列表 |
| `width` | string | 否 | default | ✅ | 选择器宽度 |
| `disabled` | boolean | 否 | false | ✅ | 是否禁用 |
| `confirm` | object | 否 | - | ✅ | 二次确认配置 |
| `behaviors` | array | 否 | - | ✅ | 交互行为列表 |

**Builder 方法**:
- `Name(string)` - 设置名称
- `Required()` - 设置必填
- `Placeholder(string)` - 设置占位文本
- `SelectedValues(params string[])` - 设置默认选中用户
- `Option(string)` - 添加候选人员
- `Width(string)` - 设置宽度
- `Disabled()` - 设置禁用

---

### 6.5 Checker (勾选器)

**支持状态**: ✅ 完整支持

**Tag**: `checker`

| 属性 | 类型 | 必填 | 默认值 | 支持状态 | 说明 |
|------|------|------|--------|----------|------|
| `tag` | string | 是 | - | ✅ | 固定为 checker |
| `name` | string | 否 | - | ✅ | 勾选器唯一标识 |
| `checked` | boolean | 否 | false | ✅ | 初始勾选状态 |
| `text` | Element | 否 | - | ✅ | 勾选器文本内容 |
| `overall_checkable` | boolean | 否 | true | ✅ | 光标悬浮时是否有阴影效果 |
| `button_area` | object | 否 | - | ✅ | 按钮区配置 |
| `checked_style` | object | 否 | - | ✅ | 勾选状态样式 |
| `margin` | string | 否 | 0px | ✅ | 外边距 |
| `padding` | string | 否 | 0px | ✅ | 内边距 |
| `confirm` | object | 否 | - | ✅ | 二次确认配置 |
| `behaviors` | array | 否 | - | ✅ | 交互行为列表 |
| `hover_tips` | PlainText | 否 | - | ✅ | 悬浮提示 |
| `disabled` | boolean | 否 | false | ✅ | 是否禁用 |
| `disabled_tips` | PlainText | 否 | - | ✅ | 禁用提示 |

**Builder 方法**:
- `Name(string)` - 设置名称
- `Checked(bool)` - 设置勾选状态
- `Text(string)` - 设置文本内容
- `MarkdownText(string)` - 设置 Markdown 文本
- `OverallCheckable(bool)` - 设置悬浮阴影效果
- `Strikethrough()` - 设置勾选时显示删除线
- `Opacity(double)` - 设置不透明度
- `Margin(string)` - 设置外边距
- `Padding(string)` - 设置内边距
- `Disabled()` - 设置禁用
- `OnCallback(object?)` - 设置回调行为

---

## 七、媒体组件支持情况

### 7.1 Image (图片)

**支持状态**: ✅ 完整支持

**Tag**: `img`

| 属性 | 类型 | 必填 | 默认值 | 支持状态 | 说明 |
|------|------|------|--------|----------|------|
| `tag` | string | 是 | - | ✅ | 固定为 img |
| `img_key` | string | 是 | - | ✅ | 图片资源的 Key |
| `alt` | PlainText | 是 | - | ✅ | 悬浮说明文案 |
| `title` | PlainText | 否 | - | ✅ | 图片标题 |
| `scale_type` | string | 否 | crop_center | ✅ | 图片裁剪模式 |
| `size` | string | 否 | - | ✅ | 图片尺寸 |
| `corner_radius` | string | 否 | - | ✅ | 图片圆角半径 |
| `transparent` | boolean | 否 | false | ✅ | 是否为透明底色 |
| `preview` | boolean | 否 | true | ✅ | 点击后是否放大图片 |
| `margin` | string | 否 | 0px | ✅ | 外边距 |

**裁剪模式枚举**:
- `crop_center` - 居中裁剪
- `crop_top` - 顶部裁剪
- `fit_horizontal` - 完整展示不裁剪

**Builder 方法**:
- `ImgKey(string)` - 设置图片 Key
- `Alt(string)` - 设置悬浮说明
- `Title(string)` - 设置标题
- `ScaleType(string)` / `ScaleType(ImageScaleType)` - 设置裁剪模式
- `Size(string)` / `Size(ImageSize)` - 设置尺寸
- `CornerRadius(string)` - 设置圆角
- `Transparent(bool)` - 设置透明底色
- `Preview(bool)` - 设置是否可放大
- `Margin(string)` - 设置外边距

---

### 7.2 ImgCombination (多图混排)

**支持状态**: ✅ 完整支持

**Tag**: `img_combination`

| 属性 | 类型 | 必填 | 默认值 | 支持状态 | 说明 |
|------|------|------|--------|----------|------|
| `tag` | string | 是 | - | ✅ | 固定为 img_combination |
| `combination_mode` | string | 是 | - | ✅ | 多图混排方式 |
| `combination_transparent` | boolean | 否 | false | ✅ | 是否为透明底色 |
| `corner_radius` | string | 否 | - | ✅ | 图片圆角半径 |
| `img_list` | ImgItem[] | 是 | - | ✅ | 图片资源数组 |
| `margin` | string | 否 | 0px | ✅ | 外边距 |

**混排方式枚举**:
- `double` - 双图混排
- `triple` - 三图混排
- `quadruple` - 四宫格
- `grid_six` - 六宫格
- `grid_nine` - 九宫格

**Builder 方法**:
- `Mode(string)` - 设置混排方式
- `Double()` / `Triple()` / `Quadruple()` / `GridSix()` / `GridNine()` - 设置混排方式
- `CornerRadius(string)` - 设置圆角
- `Transparent(bool)` - 设置透明底色
- `Image(string, bool?)` - 添加图片
- `Images(params string[])` - 批量添加图片

---

## 八、展示组件支持情况

### 8.1 Table (表格)

**支持状态**: ✅ 完整支持

**Tag**: `table`

| 属性 | 类型 | 必填 | 默认值 | 支持状态 | 说明 |
|------|------|------|--------|----------|------|
| `tag` | string | 是 | - | ✅ | 固定为 table |
| `page_size` | int | 否 | 5 | ✅ | 每页最大展示行数 |
| `row_height` | string | 否 | low | ✅ | 行高设置 |
| `row_max_height` | string | 否 | 124px | ✅ | 最大行高 |
| `freeze_first_column` | boolean | 否 | false | ✅ | 是否冻结首列 |
| `header_style` | object | 否 | - | ✅ | 表头样式 |
| `columns` | TableColumn[] | 是 | - | ✅ | 列对象数组 |
| `rows` | object[] | 是 | - | ✅ | 行对象数组 |
| `margin` | string | 否 | 0px | ✅ | 外边距 |

**数据类型支持**:
- `text` - 普通文本
- `lark_md` - Markdown 格式文本
- `options` - 选项标签
- `number` - 数字
- `persons` - 人员列表
- `date` - 日期时间
- `markdown` - 完整 Markdown 语法

**Builder 方法**:
- `PageSize(int)` - 设置每页行数
- `RowHeight(string)` - 设置行高
- `FreezeFirstColumn()` - 冻结首列
- `HeaderStyle(Action<TableHeaderStyleBuilder>)` - 设置表头样式
- `Column(Action<TableColumnBuilder>)` - 添加列
- `Row(Action<TableRowBuilder>)` - 添加行

---

### 8.2 Hr (分割线)

**支持状态**: ✅ 完整支持

**Tag**: `hr`

| 属性 | 类型 | 必填 | 默认值 | 支持状态 | 说明 |
|------|------|------|--------|----------|------|
| `tag` | string | 是 | - | ✅ | 固定为 hr |
| `element_id` | string | 否 | - | ✅ | 元素唯一标识 |
| `margin` | string | 否 | 0px | ✅ | 外边距 |

**Builder 方法**:
- `ElementId(string)` - 设置元素 ID
- `Margin(string)` - 设置外边距

---

### 8.3 Person (人员)

**支持状态**: ✅ 完整支持

**Tag**: `person`

| 属性 | 类型 | 必填 | 默认值 | 支持状态 | 说明 |
|------|------|------|--------|----------|------|
| `tag` | string | 是 | - | ✅ | 固定为 person |
| `user_id` | string | 否 | - | ✅ | 用户的 user_id |
| `open_id` | string | 否 | - | ✅ | 用户的 open_id |
| `union_id` | string | 否 | - | ✅ | 用户的 union_id |
| `style` | string | 否 | normal | ✅ | 展示样式 |
| `show_name` | boolean | 否 | true | ✅ | 是否展示用户名 |
| `show_avatar` | boolean | 否 | true | ✅ | 是否展示头像 |
| `margin` | string | 否 | 0px | ✅ | 外边距 |

**Builder 方法**:
- `UserId(string)` - 设置 user_id
- `OpenId(string)` - 设置 open_id
- `UnionId(string)` - 设置 union_id
- `Style(string)` - 设置展示样式
- `ShowName(bool)` - 设置是否展示用户名
- `ShowAvatar(bool)` - 设置是否展示头像
- `Margin(string)` - 设置外边距

---

### 8.4 PersonList (人员列表)

**支持状态**: ✅ 完整支持

**Tag**: `person_list`

| 属性 | 类型 | 必填 | 默认值 | 支持状态 | 说明 |
|------|------|------|--------|----------|------|
| `tag` | string | 是 | - | ✅ | 固定为 person_list |
| `user_ids` | string[] | 否 | - | ✅ | 用户的 user_id 列表 |
| `open_ids` | string[] | 否 | - | ✅ | 用户的 open_id 列表 |
| `union_ids` | string[] | 否 | - | ✅ | 用户的 union_id 列表 |
| `style` | string | 否 | normal | ✅ | 展示样式 |
| `margin` | string | 否 | 0px | ✅ | 外边距 |

**Builder 方法**:
- `UserIds(params string[])` - 设置 user_id 列表
- `OpenIds(params string[])` - 设置 open_id 列表
- `UnionIds(params string[])` - 设置 union_id 列表
- `Style(string)` - 设置展示样式
- `Margin(string)` - 设置外边距

---

### 8.5 Chart (图表)

**支持状态**: ✅ 完整支持

**Tag**: `chart`

| 属性 | 类型 | 必填 | 默认值 | 支持状态 | 说明 |
|------|------|------|--------|----------|------|
| `tag` | string | 是 | - | ✅ | 固定为 chart |
| `element_id` | string | 否 | - | ✅ | 元素唯一标识 |
| `chart_type` | string | 是 | - | ✅ | 图表类型 |
| `data` | object | 是 | - | ✅ | 图表数据 |
| `width` | string | 否 | fill | ✅ | 图表宽度 |
| `margin` | string | 否 | 0px | ✅ | 外边距 |

**Builder 方法**:
- `ChartType(string)` - 设置图表类型
- `Data(object)` - 设置图表数据
- `Width(string)` - 设置图表宽度
- `ElementId(string)` - 设置元素 ID
- `Margin(string)` - 设置外边距

---

### 8.6 CollapsiblePanel (折叠面板)

**支持状态**: ✅ 完整支持

**Tag**: `collapsible_panel`

| 属性 | 类型 | 必填 | 默认值 | 支持状态 | 说明 |
|------|------|------|--------|----------|------|
| `tag` | string | 是 | - | ✅ | 固定为 collapsible_panel |
| `element_id` | string | 否 | - | ✅ | 元素唯一标识 |
| `expanded` | boolean | 否 | false | ✅ | 是否展开 |
| `header` | object | 是 | - | ✅ | 面板头部配置 |
| `elements` | Element[] | 是 | - | ✅ | 子元素列表 |
| `margin` | string | 否 | 0px | ✅ | 外边距 |

**Builder 方法**:
- `Expanded(bool)` - 设置是否展开
- `Title(string)` - 设置头部标题
- `HeaderIcon(HeaderIcon)` - 设置头部图标
- `StandardIcon(string, string?)` - 设置标准图标
- `CustomIcon(string)` - 设置自定义图标
- `ExpandedIcon(HeaderIcon)` - 设置展开时的图标
- `AddElement(Element)` - 添加子元素
- `ElementId(string)` - 设置元素 ID
- `Margin(string)` - 设置外边距

---

### 8.7 SelectImg (多图选择)

**支持状态**: ✅ 完整支持

**Tag**: `select_img`

| 属性 | 类型 | 必填 | 默认值 | 支持状态 | 说明 |
|------|------|------|--------|----------|------|
| `tag` | string | 是 | - | ✅ | 固定为 select_img |
| `name` | string | 否 | - | ✅ | 选择器唯一标识 |
| `required` | boolean | 否 | false | ✅ | 是否必填 |
| `placeholder` | PlainText | 否 | - | ✅ | 占位文本 |
| `label` | PlainText | 否 | - | ✅ | 文本标签 |
| `select_mode` | string | 否 | - | ✅ | 选择模式 |
| `initial_options` | string[] | 否 | - | ✅ | 初始选中图片 |
| `options` | SelectImgOption[] | 否 | - | ✅ | 图片选项列表 |
| `width` | string | 否 | default | ✅ | 选择器宽度 |
| `disabled` | boolean | 否 | false | ✅ | 是否禁用 |
| `confirm` | object | 否 | - | ✅ | 二次确认配置 |
| `behaviors` | array | 否 | - | ✅ | 交互行为列表 |

**Builder 方法**:
- `Name(string)` - 设置名称
- `Required(bool)` - 设置必填
- `Placeholder(string)` - 设置占位文本
- `Label(string)` - 设置标签
- `SelectMode(string)` - 设置选择模式
- `Single()` - 设置单选模式
- `Multi()` - 设置多选模式
- `InitialOptions(List<string>)` - 设置初始选中图片
- `Option(string, string?)` - 添加图片选项
- `Width(string)` - 设置宽度
- `Disabled(bool)` - 设置禁用
- `Confirm(string, string)` - 设置二次确认
- `ElementId(string)` - 设置元素 ID

---

## 九、交互行为支持情况

### 9.1 Callback (回调行为)

**支持状态**: ✅ 完整支持

**Type**: `callback`

| 属性 | 类型 | 必填 | 支持状态 | 说明 |
|------|------|------|----------|------|
| `type` | string | 是 | ✅ | 固定为 callback |
| `value` | object | 是 | ✅ | 回传数据 |

**使用方式**:
```csharp
.OnCallback(new { action = "submit", id = 123 })
```

---

### 9.2 OpenUrl (打开 URL 行为)

**支持状态**: ✅ 完整支持

**Type**: `open_url`

| 属性 | 类型 | 必填 | 支持状态 | 说明 |
|------|------|------|----------|------|
| `type` | string | 是 | ✅ | 固定为 open_url |
| `default_url` | string | 是 | ✅ | 兜底跳转地址 |
| `android_url` | string | 否 | ✅ | 安卓端跳转地址 |
| `ios_url` | string | 否 | ✅ | iOS 端跳转地址 |
| `pc_url` | string | 否 | ✅ | PC 端跳转地址 |

**使用方式**:
```csharp
.OnClick("https://example.com")
// 或
.OnClick(new OpenUrlBehavior 
{ 
    DefaultUrl = "https://example.com",
    PcUrl = "https://example.com/pc"
})
```

---

### 9.3 FormSubmit (表单提交)

**支持状态**: ✅ 完整支持

**Type**: `form_submit`

| 属性 | 类型 | 必填 | 支持状态 | 说明 |
|------|------|------|----------|------|
| `type` | string | 是 | ✅ | 固定为 form_submit |

**使用方式**:
```csharp
.FormSubmit()
```

---

### 9.4 FormReset (表单重置)

**支持状态**: ✅ 完整支持

**Type**: `form_reset`

| 属性 | 类型 | 必填 | 支持状态 | 说明 |
|------|------|------|----------|------|
| `type` | string | 是 | ✅ | 固定为 form_reset |

**使用方式**:
```csharp
.FormReset()
```

---

## 十、二次确认弹窗支持情况

### 10.1 Confirm (二次确认)

**支持状态**: ✅ 完整支持

| 属性 | 类型 | 必填 | 支持状态 | 说明 |
|------|------|------|----------|------|
| `title` | PlainText | 是 | ✅ | 弹窗标题 |
| `text` | PlainText | 是 | ✅ | 弹窗正文内容 |

**使用方式**:
```csharp
.Confirm("确认提交", "提交后将无法修改，是否确认提交？")
```

---

## 十一、附录

### 11.1 版本兼容性说明

| 项目 | 说明 |
|------|------|
| 飞书客户端版本 | 7.20 及之后版本 |
| 卡片 JSON 结构版本 | 2.0 |
| .NET 版本 | .NET 10.0 |

### 11.2 后续开发计划

所有飞书卡片 2.0 标准组件已完整实现。

### 11.3 元素嵌套规则

| 父组件 | 可包含的子元素 |
|--------|----------------|
| Card Body | 所有元素类型 |
| Div | 所有元素类型 |
| Column | 所有元素类型（除表单容器和表格外） |
| Form | Input, Select, DatePicker, PickerTime, PickerDatetime, Checkbox, Button, Div, PlainText, Markdown, ColumnSet |
| ColumnSet | Column |
| InteractiveContainer | 所有元素类型（除表单容器和表格外） |

### 11.4 限制说明

- 一张卡片最多支持 200 个元素
- 容器类组件最多支持嵌套五层
- 表格组件不可被内嵌在其它组件内
- 单张卡片最多支持放置五个表格组件

---

*文档版本：1.1*
*最后更新：2026-03-10*
