# 飞书互动卡片 2.0 标准文档

> 本文档为飞书互动卡片 2.0 的完整开发参考文档，包含所有组件的 JSON 结构、字段说明、嵌套规则和示例。

---

## 目录

1. [概述](#1-概述)
2. [卡片根结构](#2-卡片根结构)
3. [基础元素类型](#3-基础元素类型)
4. [容器组件](#4-容器组件)
5. [表单组件](#5-表单组件)
6. [交互组件](#6-交互组件)
7. [媒体组件](#7-媒体组件)
8. [交互行为类型](#8-交互行为类型)
9. [组件嵌套规则](#9-组件嵌套规则)
10. [完整 JSON 示例](#10-完整 json 示例)

---

## 1. 概述

### 1.1 卡片 2.0 简介

飞书互动卡片 2.0 是一种结构化的消息格式，支持丰富的交互元素，包括文本、图片、按钮、表单等。卡片 2.0 使用 JSON 格式定义，通过 `schema: "2.0"` 标识版本。

### 1.2 注意事项

**客户端版本要求**：

- 卡片 JSON 2.0 结构支持飞书客户端 7.20 及之后版本
- 当使用 JSON 2.0 结构的卡片发送至低于 7.20 版本的客户端时，卡片标题可正常显示，但内容将展示兜底的升级提示文案

**元素数量限制**：

- 一张卡片最多支持 200 个元素（如 `tag` 为 `plain_text` 的文本元素）或组件

**嵌套层级限制**：

- 容器类组件最多支持嵌套五层组件
- 建议避免多层嵌套，会压缩内容的展示空间，影响卡片的展示效果

**共享卡片限制**：

- 卡片 JSON 2.0 结构暂时仅支持共享卡片，不支持独享卡片配置
- 即 `update_multi` 参数仅支持设为 `true`

**交互有效期变更**（与 1.0 的差异）：

- JSON 2.0 结构中，发出卡片的可交互时间和可更新时间有所调整，详情参考飞书开放平台文档

### 1.3 卡片层级结构

```
Card (卡片)
├── config (配置)
├── header (头部)
├── body (主体)
│   └── elements[] (元素列表)
│       ├── div (容器)
│       ├── button (按钮)
│       ├── column-set (分栏)
│       ├── form (表单)
│       ├── input (输入框)
│       ├── select (选择器)
│       ├── datepicker (日期选择器)
│       ├── checkbox (勾选框)
│       ├── img (图片)
│       ├── markdown (Markdown 文本)
│       └── plain_text (纯文本)
└── fallback (降级内容)
```

---

## 2. 卡片根结构

### 2.1 Card (卡片根对象)

| 字段          | 类型   | 必填 | 说明                                 |
| ------------- | ------ | ---- | ------------------------------------ |
| `schema`    | string | 是   | 卡片 JSON 结构版本，固定为 `"2.0"` |
| `config`    | object | 否   | 卡片配置                             |
| `card_link` | object | 否   | 卡片链接配置                         |
| `header`    | object | 否   | 卡片头部                             |
| `body`      | object | 是   | 卡片主体                             |
| `fallback`  | object | 否   | 降级配置                             |

**JSON 示例**:

```json
{
  "schema": "2.0",
  "config": {
    "update_multi": true
  },
  "header": {
    "title": {
      "tag": "plain_text",
      "content": "卡片标题"
    }
  },
  "body": {
    "elements": [
      {
        "tag": "plain_text",
        "content": "卡片内容"
      }
    ]
  }
}
```

---

### 2.2 Config (卡片配置)

`config` 用于配置卡片的全局行为，包括流式更新模式、是否允许被转发、是否为共享卡片等。

| 字段                           | 类型     | 必填 | 默认值      | 说明                                                 |
| ------------------------------ | -------- | ---- | ----------- | ---------------------------------------------------- |
| `streaming_mode`             | boolean  | 否   | `false`   | 卡片是否处于流式更新模式                             |
| `streaming_config`           | object   | 否   | -           | 流式更新配置                                         |
| `summary`                    | object   | 否   | -           | 卡片摘要信息，自定义客户端聊天栏消息预览中的展示文案 |
| `locales`                    | string[] | 否   | -           | 指定生效的语言列表                                   |
| `enable_forward`             | boolean  | 否   | `true`    | 是否支持转发卡片                                     |
| `update_multi`               | boolean  | 否   | `true`    | 是否为共享卡片（JSON 2.0 暂时仅支持设为 `true`）   |
| `width_mode`                 | string   | 否   | `default` | 卡片宽度模式：`default`/`compact`/`fill`       |
| `use_custom_translation`     | boolean  | 否   | `false`   | 是否使用自定义翻译数据                               |
| `enable_forward_interaction` | boolean  | 否   | `false`   | 转发的卡片是否仍然支持回传交互                       |
| `style`                      | object   | 否   | -           | 自定义字号和颜色配置                                 |

**streaming_config (流式更新配置)**:

| 字段                   | 类型   | 说明                                                  |
| ---------------------- | ------ | ----------------------------------------------------- |
| `print_frequency_ms` | object | 流式更新频率（毫秒），可配置 default/android/ios/pc   |
| `print_step`         | object | 流式更新步长（字符数），可配置 default/android/ios/pc |
| `print_strategy`     | string | 流式更新策略：`fast`/`delay`                      |

**summary (摘要配置)**:

| 字段             | 类型   | 说明                 |
| ---------------- | ------ | -------------------- |
| `content`      | string | 自定义摘要信息       |
| `i18n_content` | object | 摘要信息的多语言配置 |

**style (样式配置)**:

| 字段          | 类型   | 说明                                    |
| ------------- | ------ | --------------------------------------- |
| `text_size` | object | 自定义字号，可配置 default/pc/mobile    |
| `color`     | object | 自定义颜色，可配置 light_mode/dark_mode |

**width_mode 枚举值**:

| 值          | 说明                                             |
| ----------- | ------------------------------------------------ |
| `default` | 默认宽度，PC 端宽版、iPad 端上的宽度上限为 600px |
| `compact` | 紧凑宽度 400px                                   |
| `fill`    | 自适应屏幕宽度                                   |

**JSON 示例**:

```json
{
  "config": {
    "update_multi": true,
    "streaming_mode": false,
    "enable_forward": true,
    "width_mode": "fill",
    "summary": {
      "content": "自定义摘要内容"
    },
    "style": {
      "text_size": {
        "cus-0": {
          "default": "medium",
          "pc": "medium",
          "mobile": "large"
        }
      },
      "color": {
        "cus-0": {
          "light_mode": "rgba(5,157,178,0.52)",
          "dark_mode": "rgba(78,23,108,0.49)"
        }
      }
    }
  }
}
```

---

### 2.3 card_link (卡片全局跳转链接)

`card_link` 字段用于指定卡片整体的点击跳转链接。你可以配置一个默认链接，也可以分别为 PC 端、Android 端、iOS 端配置不同的跳转链接。

| 字段            | 类型   | 必填 | 说明                 |
| --------------- | ------ | ---- | -------------------- |
| `url`         | string | 否   | 默认的链接地址       |
| `pc_url`      | string | 否   | PC 端的链接地址      |
| `ios_url`     | string | 否   | iOS 端的链接地址     |
| `android_url` | string | 否   | Android 端的链接地址 |

**注意事项**：

- `url` 和各端的链接（`android_url`、`ios_url`、`pc_url`）必填其中一个
- 如果不填写 `url`，则必须完整填写 `android_url`、`ios_url`、`pc_url` 三个字段
- 如果同时填写了 `url` 和各端链接，`url` 字段优先级更高
- 如果需要禁止某端进行跳转，可以将对应的参数值配置为 `lark://msgcard/unsupported_action`

**JSON 示例**:

```json
{
  "card_link": {
    "url": "https://example.com",
    "pc_url": "https://example.com/pc",
    "ios_url": "https://example.com/ios",
    "android_url": "https://example.com/android"
  }
}
```

---

### 2.4 Header (卡片头部)

`header` 字段用于配置卡片的标题，支持添加卡片主标题、副标题、后缀标签和标题图标。

| 字段              | 类型      | 必填 | 默认值      | 说明                            |
| ----------------- | --------- | ---- | ----------- | ------------------------------- |
| `title`         | PlainText | 是   | -           | 头部主标题                      |
| `subtitle`      | PlainText | 否   | -           | 头部副标题                      |
| `icon`          | object    | 否   | -           | 头部图标配置                    |
| `template`      | string    | 否   | `default` | 标题主题样式颜色                |
| `text_tag_list` | array     | 否   | -           | 标题后缀标签，最多设置 3 个标签 |
| `padding`       | string    | 否   | `12px`    | 头部内边距，支持范围 [0,99]px   |

**icon (图标配置)**:

| 字段        | 类型   | 必填 | 说明                                                                |
| ----------- | ------ | ---- | ------------------------------------------------------------------- |
| `tag`     | string | 是   | 图标类型：`standard_icon`（图标库）/`custom_icon`（自定义图片） |
| `token`   | string | 否   | 图标库中的图标 token（tag 为 standard_icon 时生效）                 |
| `color`   | string | 否   | 图标颜色（tag 为 standard_icon 时生效）                             |
| `img_key` | string | 否   | 自定义图标的图片 key（tag 为 custom_icon 时生效）                   |

**template (标题主题样式颜色) 枚举值**:

| 值            | 说明     |
| ------------- | -------- |
| `default`   | 默认样式 |
| `blue`      | 蓝色     |
| `wathet`    | 天蓝色   |
| `turquoise` | 青绿色   |
| `green`     | 绿色     |
| `yellow`    | 黄色     |
| `orange`    | 橙色     |
| `red`       | 红色     |
| `carmine`   | 洋红色   |
| `violet`    | 紫罗兰色 |
| `purple`    | 紫色     |
| `indigo`    | 靛青色   |
| `grey`      | 灰色     |

**text_tag_list (标题后缀标签)**:

| 字段           | 类型      | 必填 | 说明                |
| -------------- | --------- | ---- | ------------------- |
| `tag`        | string    | 是   | 固定为 `text_tag` |
| `element_id` | string    | 否   | 元素唯一标识        |
| `text`       | PlainText | 是   | 标签内容            |
| `color`      | string    | 否   | 标签颜色            |

**JSON 示例**:

```json
{
  "header": {
    "title": {
      "tag": "plain_text",
      "content": "欢迎使用飞书卡片"
    },
    "subtitle": {
      "tag": "plain_text",
      "content": "这是副标题"
    },
    "template": "blue",
    "icon": {
      "tag": "standard_icon",
      "token": "chat_outlined",
      "color": "blue"
    },
    "text_tag_list": [
      {
        "tag": "text_tag",
        "text": {
          "tag": "plain_text",
          "content": "标签1"
        },
        "color": "blue"
      }
    ],
    "padding": "12px"
  }
}
```

---

### 2.5 Body (卡片主体)

`body` 字段是卡片的正文部分，包含一个名为 `elements` 的数组，用于放置各类组件。

| 字段                   | 类型      | 必填 | 默认值       | 说明                                                          |
| ---------------------- | --------- | ---- | ------------ | ------------------------------------------------------------- |
| `elements`           | Element[] | 是   | -            | 卡片元素列表                                                  |
| `direction`          | string    | 否   | `vertical` | 正文或容器内组件的排列方向：`vertical`/`horizontal`       |
| `padding`            | string    | 否   | `12px`     | 正文或容器内组件的内边距，支持范围 [0,99]px                   |
| `horizontal_spacing` | string    | 否   | `8px`      | 正文或容器内组件的水平间距                                    |
| `horizontal_align`   | string    | 否   | `left`     | 正文或容器内组件的水平对齐方式：`left`/`center`/`right` |
| `vertical_spacing`   | string    | 否   | `8px`      | 正文或容器内组件的垂直间距                                    |
| `vertical_align`     | string    | 否   | `top`      | 正文或容器内组件的垂直对齐方式：`top`/`center`/`bottom` |

**spacing 间距枚举值**:

| 值              | 说明           |
| --------------- | -------------- |
| `small`       | 小间距，4px    |
| `medium`      | 中等间距，8px  |
| `large`       | 大间距，12px   |
| `extra_large` | 超大间距，16px |
| `[0,99]px`    | 自定义间距值   |

**JSON 示例**:

```json
{
  "body": {
    "direction": "vertical",
    "padding": "12px",
    "horizontal_spacing": "8px",
    "vertical_spacing": "8px",
    "horizontal_align": "left",
    "vertical_align": "top",
    "elements": [
      {
        "tag": "plain_text",
        "content": "元素 1"
      },
      {
        "tag": "plain_text",
        "content": "元素 2"
      }
    ]
  }
}
```

---

### 2.5 基础元素属性 (Base Element)

所有元素都继承以下基础属性：

| 字段           | 类型   | 必填 | 说明                   |
| -------------- | ------ | ---- | ---------------------- |
| `tag`        | string | 是   | 元素标签名（组件类型） |
| `element_id` | string | 否   | 元素唯一标识           |
| `margin`     | string | 否   | 外边距                 |

---

## 3. 基础元素类型

### 3.1 PlainText (纯文本)

纯文本组件用于展示普通文本内容，支持设置字号、颜色、对齐方式等样式。

| 字段           | 类型    | 必填 | 默认值      | 说明                               |
| -------------- | ------- | ---- | ----------- | ---------------------------------- |
| `tag`        | string  | 是   | -           | 固定为 `"plain_text"`            |
| `content`    | string  | 是   | -           | 文本内容（最多 100 个字符）        |
| `text_size`  | string  | 否   | `normal`  | 文本大小                           |
| `text_color` | string  | 否   | `default` | 文本颜色                           |
| `text_align` | string  | 否   | `left`    | 文本对齐方式                       |
| `notation`   | boolean | 否   | `false`   | 是否使用 notation 样式（灰色小字） |
| `width`      | string  | 否   | -           | 文本宽度                           |

**text_size (文本大小) 枚举值**:

| 值            | 说明             |
| ------------- | ---------------- |
| `heading-0` | 特大标题（30px） |
| `heading-1` | 一级标题（24px） |
| `heading-2` | 二级标题（20px） |
| `heading-3` | 三级标题（18px） |
| `heading-4` | 四级标题（16px） |
| `heading`   | 标题（16px）     |
| `normal`    | 正文（14px）     |
| `notation`  | 辅助信息（12px） |

**text_align (文本对齐) 枚举值**:

| 值         | 说明     |
| ---------- | -------- |
| `left`   | 左对齐   |
| `center` | 居中对齐 |
| `right`  | 右对齐   |

**JSON 示例**:

```json
{
  "tag": "plain_text",
  "content": "这是纯文本内容",
  "text_size": "normal",
  "text_color": "default",
  "text_align": "left"
}
```

---

### 3.2 Markdown (富文本)

富文本（Markdown）组件支持渲染文本、图片、分割线等元素。JSON 2.0 结构支持除 `SetextHeading`、`CodeBlock` 和 `HTMLBlock` 外所有标准的 Markdown 语法，以及部分 HTML 语法。

| 字段           | 类型   | 必填 | 默认值      | 说明                  |
| -------------- | ------ | ---- | ----------- | --------------------- |
| `tag`        | string | 是   | -           | 固定为 `"markdown"` |
| `content`    | string | 是   | -           | Markdown 内容         |
| `text_size`  | string | 否   | `normal`  | 文本大小              |
| `text_color` | string | 否   | `default` | 文本颜色              |
| `text_align` | string | 否   | `left`    | 文本对齐方式          |
| `icon`       | object | 否   | -           | 前缀图标              |

**支持的 Markdown 语法**:

| 名称       | 语法                                                         | 效果           |
| ---------- | ------------------------------------------------------------ | -------------- |
| 换行       | `<br>` 或 `\n`                                           | 换行           |
| 斜体       | `*斜体*`                                                   | *斜体*       |
| 粗体       | `**粗体**` 或 `__粗体__`                                 | **粗体** |
| 删除线     | `~~删除线~~`                                               | ~~删除线~~    |
| 超链接     | `[文本](链接)`                                             | [文本](链接)      |
| 图片       | `![hover_text](image_key)`                                 | 显示图片       |
| 分割线     | `---`                                                      | 水平分割线     |
| 无序列表   | `- 项目`                                                   | 无序列表       |
| 有序列表   | `1. 项目`                                                  | 有序列表       |
| 引用       | `> 引用内容`                                               | 引用块         |
| 代码块     | ` ```语言\n代码\n``` `                                     | 代码块         |
| @指定人    | `<at id=open_id></at>`                                     | @用户名        |
| @所有人    | `<at id=all></at>`                                         | @所有人        |
| 彩色文本   | `<font color='red'>文本</font>`                            | 彩色文本       |
| 标签       | `<text_tag color='blue'>标签</text_tag>`                   | 标签样式       |
| 人员       | `<person id='user_id'></person>`                           | 人员展示       |
| 含图标链接 | `<link icon='chat_outlined' url='https://...'>文本</link>` | 带图标链接     |

**JSON 示例**:

```json
{
  "tag": "markdown",
  "content": "**粗体文本**\n*斜体文本*\n- 列表项 1\n- 列表项 2\n[链接](https://example.com)",
  "text_size": "normal",
  "text_align": "left"
}
```

---

## 4. 容器组件

### 4.1 Div (容器)

| 字段                   | 类型      | 必填 | 默认值         | 说明                                                |
| ---------------------- | --------- | ---- | -------------- | --------------------------------------------------- |
| `tag`                | string    | 是   | -              | 固定为 `"div"`                                    |
| `text`               | Element   | 否   | -              | 飞书卡片 2.0 格式的文本内容 (PlainText 或 Markdown) |
| `elements`           | Element[] | 否   | -              | 子元素列表（旧格式）                                |
| `direction`          | string    | 否   | `"vertical"` | 布局方向：`"vertical"` 或 `"horizontal"`        |
| `padding`            | string    | 否   | -              | 内边距                                              |
| `vertical_spacing`   | string    | 否   | -              | 垂直间距                                            |
| `horizontal_spacing` | string    | 否   | -              | 水平间距                                            |
| `horizontal_align`   | string    | 否   | -              | 水平对齐：`"left"`/`"center"`/`"right"`       |
| `vertical_align`     | string    | 否   | -              | 垂直对齐：`"top"`/`"center"`/`"bottom"`       |
| `width`              | string    | 否   | -              | 文本宽度                                            |

**可嵌套在**: Card Body, Div, Column, Form

**可包含子元素**: 所有元素类型

**JSON 示例 (使用 text 字段)**:

```json
{
  "tag": "div",
  "text": {
    "tag": "plain_text",
    "content": "这是 div 的文本内容"
  }
}
```

**JSON 示例 (使用 elements 字段)**:

```json
{
  "tag": "div",
  "direction": "vertical",
  "elements": [
    {
      "tag": "plain_text",
      "content": "第一行"
    },
    {
      "tag": "plain_text",
      "content": "第二行"
    }
  ]
}
```

---

### 4.2 ColumnSet (分栏布局)

| 字段                   | 类型     | 必填 | 说明                    |
| ---------------------- | -------- | ---- | ----------------------- |
| `tag`                | string   | 是   | 固定为 `"column_set"` |
| `columns`            | Column[] | 是   | 分栏列列表              |
| `margin`             | string   | 否   | 外边距                  |
| `background_style`   | string   | 否   | 背景样式                |
| `horizontal_spacing` | string   | 否   | 水平间距                |

**Column (列) 结构**:

| 字段               | 类型      | 必填 | 默认值       | 说明                                      |
| ------------------ | --------- | ---- | ------------ | ----------------------------------------- |
| `tag`            | string    | 是   | `"column"` |                                           |
| `width`          | string    | 否   | `"auto"`   | 列宽度：`"auto"` 或自定义值如 `"50%"` |
| `vertical_align` | string    | 否   | -            | 垂直对齐方式                              |
| `elements`       | Element[] | 是   | -            | 列内元素列表                              |

**可嵌套在**: Card Body, Form

**可包含子元素**: Column 元素

**JSON 示例**:

```json
{
  "tag": "column_set",
  "columns": [
    {
      "tag": "column",
      "width": "50%",
      "elements": [
        {
          "tag": "plain_text",
          "content": "左侧列内容"
        }
      ]
    },
    {
      "tag": "column",
      "width": "50%",
      "elements": [
        {
          "tag": "plain_text",
          "content": "右侧列内容"
        }
      ]
    }
  ]
}
```

---

## 5. 表单组件

### 5.1 Form (表单)

| 字段                   | 类型      | 必填 | 默认值         | 说明              |
| ---------------------- | --------- | ---- | -------------- | ----------------- |
| `tag`                | string    | 是   | -              | 固定为 `"form"` |
| `name`               | string    | 是   | -              | 表单容器唯一标识  |
| `direction`          | string    | 否   | `"vertical"` | 布局方向          |
| `padding`            | string    | 否   | -              | 内边距            |
| `vertical_spacing`   | string    | 否   | -              | 垂直间距          |
| `horizontal_spacing` | string    | 否   | -              | 水平间距          |
| `horizontal_align`   | string    | 否   | -              | 水平对齐方式      |
| `vertical_align`     | string    | 否   | -              | 垂直对齐方式      |
| `elements`           | Element[] | 是   | -              | 子元素列表        |

**可嵌套在**: Card Body

**可包含子元素**: Input, Select, DatePicker, Checkbox, Button, Div, PlainText, Markdown, ColumnSet

**JSON 示例**:

```json
{
  "tag": "form",
  "name": "myForm",
  "direction": "vertical",
  "elements": [
    {
      "tag": "input",
      "name": "username",
      "label": {
        "tag": "plain_text",
        "content": "用户名"
      }
    }
  ]
}
```

---

### 5.2 Input (输入框)

输入框组件支持收集不固定的文本内容，如原因、评价、备注等。

| 字段               | 类型      | 必填 | 默认值      | 说明                                               |
| ------------------ | --------- | ---- | ----------- | -------------------------------------------------- |
| `tag`            | string    | 是   | -           | 固定为 `"input"`                                 |
| `element_id`     | string    | 否   | -           | 元素唯一标识                                       |
| `name`           | string    | 否   | -           | 输入框唯一标识（表单中必填）                       |
| `required`       | boolean   | 否   | `false`   | 是否必填（表单中生效）                             |
| `placeholder`    | PlainText | 否   | -           | 占位文本                                           |
| `default_value`  | string    | 否   | -           | 默认值                                             |
| `input_type`     | string    | 否   | `text`    | 输入类型：`text`/`multiline_text`/`password` |
| `label`          | PlainText | 否   | -           | 文本标签                                           |
| `label_position` | string    | 否   | `top`     | 标签位置：`top`/`left`                         |
| `max_length`     | int       | 否   | `1000`    | 最大长度，取值范围 1-1000                          |
| `rows`           | int       | 否   | `5`       | 默认行数（多行时）                                 |
| `auto_resize`    | boolean   | 否   | `false`   | 是否自适应高度（仅在 PC 端生效）                   |
| `max_rows`       | int       | 否   | -           | 最大行数                                           |
| `show_icon`      | boolean   | 否   | `true`    | 当输入类型为密码类型时，是否展示前缀图标           |
| `width`          | string    | 否   | `default` | 输入框宽度：`default`/`fill`/自定义值          |
| `disabled`       | boolean   | 否   | `false`   | 是否禁用                                           |
| `disabled_tips`  | PlainText | 否   | -           | 禁用提示                                           |
| `confirm`        | object    | 否   | -           | 二次确认配置                                       |
| `behaviors`      | array     | 否   | -           | 交互行为列表                                       |

**可嵌套在**: Card Body, Form

**JSON 示例**:

```json
{
  "tag": "input",
  "name": "username",
  "label": {
    "tag": "plain_text",
    "content": "用户名"
  },
  "placeholder": {
    "tag": "plain_text",
    "content": "请输入用户名"
  },
  "required": true,
  "input_type": "text"
}
```

---

### 5.3 select_static (下拉选择-单选)

下拉选择-单选组件支持自定义单选菜单的选项文本、图标和回传参数。

| 字段               | 类型           | 必填 | 默认值      | 说明                         |
| ------------------ | -------------- | ---- | ----------- | ---------------------------- |
| `tag`            | string         | 是   | -           | 固定为 `"select_static"`   |
| `element_id`     | string         | 否   | -           | 元素唯一标识                 |
| `name`           | string         | 否   | -           | 选择器唯一标识（表单中必填） |
| `required`       | boolean        | 否   | `false`   | 是否必填                     |
| `placeholder`    | PlainText      | 否   | -           | 占位文本                     |
| `label`          | PlainText      | 否   | -           | 文本标签                     |
| `initial_option` | string         | 否   | -           | 初始选项值                   |
| `options`        | SelectOption[] | 否   | -           | 选项列表                     |
| `width`          | string         | 否   | `default` | 选择器宽度                   |
| `disabled`       | boolean        | 否   | `false`   | 是否禁用                     |
| `disabled_tips`  | PlainText      | 否   | -           | 禁用提示                     |
| `confirm`        | object         | 否   | -           | 二次确认配置                 |
| `behaviors`      | array          | 否   | -           | 交互行为列表                 |

**SelectOption (选项) 结构**:

| 字段      | 类型      | 必填 | 说明     |
| --------- | --------- | ---- | -------- |
| `value` | string    | 是   | 选项值   |
| `text`  | PlainText | 是   | 选项文本 |
| `icon`  | object    | 否   | 选项图标 |

**可嵌套在**: Card Body, Column, Form, CollapsiblePanel

**JSON 示例**:

```json
{
  "tag": "select_static",
  "name": "city",
  "label": {
    "tag": "plain_text",
    "content": "城市"
  },
  "placeholder": {
    "tag": "plain_text",
    "content": "请选择城市"
  },
  "options": [
    {
      "value": "beijing",
      "text": {
        "tag": "plain_text",
        "content": "北京"
      }
    },
    {
      "value": "shanghai",
      "text": {
        "tag": "plain_text",
        "content": "上海"
      }
    }
  ]
}
```

---

### 5.4 multi_select_static (下拉选择-多选)

下拉选择-多选组件支持自定义多选菜单的选项文本、图标和回传参数。

| 字段                | 类型           | 必填 | 默认值      | 说明                             |
| ------------------- | -------------- | ---- | ----------- | -------------------------------- |
| `tag`             | string         | 是   | -           | 固定为 `"multi_select_static"` |
| `element_id`      | string         | 否   | -           | 元素唯一标识                     |
| `name`            | string         | 否   | -           | 选择器唯一标识（表单中必填）     |
| `required`        | boolean        | 否   | `false`   | 是否必填                         |
| `placeholder`     | PlainText      | 否   | -           | 占位文本                         |
| `label`           | PlainText      | 否   | -           | 文本标签                         |
| `initial_options` | string[]       | 否   | -           | 初始选中值列表                   |
| `options`         | SelectOption[] | 否   | -           | 选项列表                         |
| `width`           | string         | 否   | `default` | 选择器宽度                       |
| `disabled`        | boolean        | 否   | `false`   | 是否禁用                         |
| `disabled_tips`   | PlainText      | 否   | -           | 禁用提示                         |
| `confirm`         | object         | 否   | -           | 二次确认配置                     |
| `behaviors`       | array          | 否   | -           | 交互行为列表                     |

**可嵌套在**: Card Body, Column, Form, CollapsiblePanel

**JSON 示例**:

```json
{
  "tag": "multi_select_static",
  "name": "cities",
  "placeholder": {
    "tag": "plain_text",
    "content": "请选择城市（可多选）"
  },
  "options": [
    {
      "value": "beijing",
      "text": {
        "tag": "plain_text",
        "content": "北京"
      }
    },
    {
      "value": "shanghai",
      "text": {
        "tag": "plain_text",
        "content": "上海"
      }
    }
  ]
}
```

---

### 5.5 DatePicker (日期选择器)

---

### 5.5 DatePicker (日期选择器)

日期选择器组件是用于提供日期选项的交互组件。

| 字段              | 类型      | 必填 | 默认值      | 说明                          |
| ----------------- | --------- | ---- | ----------- | ----------------------------- |
| `tag`           | string    | 是   | -           | 固定为 `"date_picker"`      |
| `element_id`    | string    | 否   | -           | 元素唯一标识                  |
| `name`          | string    | 否   | -           | 唯一标识（表单中必填）        |
| `required`      | boolean   | 否   | `false`   | 是否必填                      |
| `placeholder`   | PlainText | 否   | -           | 占位文本                      |
| `initial_date`  | string    | 否   | -           | 初始日期，格式 `yyyy-MM-dd` |
| `width`         | string    | 否   | `default` | 宽度                          |
| `disabled`      | boolean   | 否   | `false`   | 是否禁用                      |
| `disabled_tips` | PlainText | 否   | -           | 禁用提示                      |
| `confirm`       | object    | 否   | -           | 二次确认配置                  |
| `behaviors`     | array     | 否   | -           | 交互行为列表                  |

**可嵌套在**: Card Body, Column, Form, CollapsiblePanel

**JSON 示例**:

```json
{
  "tag": "date_picker",
  "name": "birthday",
  "placeholder": {
    "tag": "plain_text",
    "content": "请选择日期"
  },
  "initial_date": "2000-01-01"
}
```

---

### 5.6 PickerTime (时间选择器)

时间选择器组件是用于提供时间选项的交互组件。

| 字段             | 类型      | 必填 | 默认值      | 说明                     |
| ---------------- | --------- | ---- | ----------- | ------------------------ |
| `tag`          | string    | 是   | -           | 固定为 `"picker_time"` |
| `element_id`   | string    | 否   | -           | 元素唯一标识             |
| `name`         | string    | 否   | -           | 唯一标识（表单中必填）   |
| `required`     | boolean   | 否   | `false`   | 是否必填                 |
| `placeholder`  | PlainText | 否   | -           | 占位文本                 |
| `initial_time` | string    | 否   | -           | 初始时间，格式 `HH:mm` |
| `width`        | string    | 否   | `default` | 宽度                     |
| `disabled`     | boolean   | 否   | `false`   | 是否禁用                 |
| `value`        | object    | 否   | -           | 回传数据                 |
| `confirm`      | object    | 否   | -           | 二次确认配置             |
| `behaviors`    | array     | 否   | -           | 交互行为列表             |

**可嵌套在**: Card Body, Column, Form, CollapsiblePanel

**JSON 示例**:

```json
{
  "tag": "picker_time",
  "name": "meeting_time",
  "placeholder": {
    "tag": "plain_text",
    "content": "请选择时间"
  },
  "initial_time": "09:00"
}
```

---

### 5.7 PickerDatetime (日期时间选择器)

日期时间选择器组件是用于提供日期和时间选项的交互组件。

| 字段                 | 类型      | 必填 | 默认值      | 说明                                    |
| -------------------- | --------- | ---- | ----------- | --------------------------------------- |
| `tag`              | string    | 是   | -           | 固定为 `"picker_datetime"`            |
| `element_id`       | string    | 否   | -           | 元素唯一标识                            |
| `name`             | string    | 否   | -           | 唯一标识（表单中必填）                  |
| `required`         | boolean   | 否   | `false`   | 是否必填                                |
| `placeholder`      | PlainText | 否   | -           | 占位文本                                |
| `initial_datetime` | string    | 否   | -           | 初始日期时间，格式 `yyyy-MM-dd HH:mm` |
| `width`            | string    | 否   | `default` | 宽度                                    |
| `disabled`         | boolean   | 否   | `false`   | 是否禁用                                |
| `value`            | object    | 否   | -           | 回传数据                                |
| `confirm`          | object    | 否   | -           | 二次确认配置                            |
| `behaviors`        | array     | 否   | -           | 交互行为列表                            |

**可嵌套在**: Card Body, Column, Form, CollapsiblePanel

**JSON 示例**:

```json
{
  "tag": "picker_datetime",
  "name": "meeting_datetime",
  "placeholder": {
    "tag": "plain_text",
    "content": "请选择日期时间"
  },
  "initial_datetime": "2024-01-01 09:00"
}
```

---

### 5.8 Checkbox (勾选框)

| 字段                         | 类型             | 必填 | 默认值    | 说明                   |
| ---------------------------- | ---------------- | ---- | --------- | ---------------------- |
| `tag`                      | string           | 是   | -         | 固定为 `"checkbox"`  |
| `name`                     | string           | 否   | -         | 唯一标识（表单中必填） |
| `required`                 | boolean          | 否   | `false` | 是否必填               |
| `options`                  | CheckboxOption[] | 否   | -         | 选项列表               |
| `initial_selected_options` | string[]         | 否   | -         | 初始选中值             |
| `behaviors`                | array            | 否   | -         | 交互行为列表           |

**CheckboxOption (选项) 结构**:

| 字段      | 类型      | 必填 | 说明     |
| --------- | --------- | ---- | -------- |
| `value` | string    | 是   | 选项值   |
| `text`  | PlainText | 是   | 选项文本 |

**可嵌套在**: Card Body, Form

**JSON 示例**:

```json
{
  "tag": "checkbox",
  "name": "hobbies",
  "options": [
    {
      "value": "reading",
      "text": {
        "tag": "plain_text",
        "content": "阅读"
      }
    },
    {
      "value": "gaming",
      "text": {
        "tag": "plain_text",
        "content": "游戏"
      }
    },
    {
      "value": "music",
      "text": {
        "tag": "plain_text",
        "content": "音乐"
      }
    }
  ],
  "initial_selected_options": ["reading"]
}
```

---

## 6. 交互组件

### 6.1 Button (按钮)

按钮组件是一种交互组件，支持多种样式和尺寸，并支持添加图标作为前缀图标。

| 字段                 | 类型      | 必填 | 默认值      | 说明                                                   |
| -------------------- | --------- | ---- | ----------- | ------------------------------------------------------ |
| `tag`              | string    | 是   | -           | 固定为 `"button"`                                    |
| `element_id`       | string    | 否   | -           | 元素唯一标识                                           |
| `type`             | string    | 否   | `default` | 按钮类型                                               |
| `size`             | string    | 否   | `medium`  | 按钮尺寸                                               |
| `width`            | string    | 否   | `default` | 按钮宽度：`default`/`fill`/自定义值                |
| `text`             | PlainText | 是   | -           | 按钮文本（最多 100 个字符）                            |
| `icon`             | object    | 否   | -           | 前缀图标                                               |
| `hover_tips`       | PlainText | 否   | -           | 悬浮提示（PC 端）                                      |
| `disabled`         | boolean   | 否   | `false`   | 是否禁用                                               |
| `disabled_tips`    | PlainText | 否   | -           | 禁用提示                                               |
| `confirm`          | object    | 否   | -           | 二次确认配置                                           |
| `behaviors`        | array     | 否   | -           | 交互行为列表                                           |
| `name`             | string    | 否   | -           | 表单容器内组件的唯一标识（表单中必填）                 |
| `form_action_type` | string    | 否   | -           | 内嵌在表单容器中的按钮的交互类型：`submit`/`reset` |

**Type (按钮类型) 枚举值**:

| 值                 | 说明                             |
| ------------------ | -------------------------------- |
| `default`        | 默认类型（黑色字体，有边框）     |
| `primary`        | 主要类型（蓝色字体，有边框）     |
| `danger`         | 危险类型（红色字体，有边框）     |
| `text`           | 文本类型（黑色字体，无边框）     |
| `primary_text`   | 主要文本类型（蓝色字体，无边框） |
| `danger_text`    | 危险文本类型（红色字体，无边框） |
| `primary_filled` | 主要填充类型（蓝底白字）         |
| `danger_filled`  | 危险填充类型（红底白字）         |
| `laser`          | 镭射按钮                         |

**Size (尺寸) 枚举值**:

| 值         | PC 尺寸 | 移动端尺寸 |
| ---------- | ------- | ---------- |
| `tiny`   | 24px    | 28px       |
| `small`  | 28px    | 28px       |
| `medium` | 32px    | 36px       |
| `large`  | 40px    | 48px       |

**可嵌套在**: Card Body, Div, Column, Form

**JSON 示例 (带回调行为)**:

```json
{
  "tag": "button",
  "text": {
    "tag": "plain_text",
    "content": "提交"
  },
  "type": "primary",
  "size": "medium",
  "behaviors": [
    {
      "type": "callback",
      "value": {
        "action": "submit"
      }
    }
  ]
}
```

**JSON 示例 (带跳转链接)**:

```json
{
  "tag": "button",
  "text": {
    "tag": "plain_text",
    "content": "查看详情"
  },
  "type": "primary",
  "behaviors": [
    {
      "type": "open_url",
      "default_url": "https://example.com",
      "pc_url": "https://example.com/pc",
      "ios_url": "https://example.com/ios",
      "android_url": "https://example.com/android"
    }
  ]
}
```

**JSON 示例 (表单提交按钮)**:

```json
{
  "tag": "button",
  "text": {
    "tag": "plain_text",
    "content": "提交表单"
  },
  "type": "primary",
  "name": "submit_btn",
  "form_action_type": "submit"
}
```

---

### 6.2 Overflow (折叠按钮组)

折叠按钮组组件支持将多个按钮添加在折叠按钮组中，默认情况下按钮组为折叠状态，点击按钮组将会展示组内所有按钮。

| 字段           | 类型             | 必填 | 默认值      | 说明                  |
| -------------- | ---------------- | ---- | ----------- | --------------------- |
| `tag`        | string           | 是   | -           | 固定为 `"overflow"` |
| `element_id` | string           | 否   | -           | 元素唯一标识          |
| `options`    | OverflowOption[] | 是   | -           | 选项列表              |
| `width`      | string           | 否   | `default` | 宽度                  |
| `disabled`   | boolean          | 否   | `false`   | 是否禁用              |
| `behaviors`  | array            | 否   | -           | 交互行为列表          |

**OverflowOption (选项) 结构**:

| 字段      | 类型      | 必填 | 说明     |
| --------- | --------- | ---- | -------- |
| `value` | string    | 是   | 选项值   |
| `text`  | PlainText | 是   | 选项文本 |
| `icon`  | object    | 否   | 选项图标 |

**可嵌套在**: Card Body, Column, Form

**JSON 示例**:

```json
{
  "tag": "overflow",
  "options": [
    {
      "value": "edit",
      "text": {
        "tag": "plain_text",
        "content": "编辑"
      }
    },
    {
      "value": "delete",
      "text": {
        "tag": "plain_text",
        "content": "删除"
      }
    }
  ]
}
```

---

### 6.3 select_person (人员选择-单选)

人员选择-单选组件支持添加指定人员作为单选选项。

| 字段               | 类型      | 必填 | 默认值      | 说明                         |
| ------------------ | --------- | ---- | ----------- | ---------------------------- |
| `tag`            | string    | 是   | -           | 固定为 `"select_person"`   |
| `element_id`     | string    | 否   | -           | 元素唯一标识                 |
| `name`           | string    | 否   | -           | 选择器唯一标识（表单中必填） |
| `required`       | boolean   | 否   | `false`   | 是否必填                     |
| `placeholder`    | PlainText | 否   | -           | 占位文本                     |
| `initial_option` | string    | 否   | -           | 初始选中用户的 open_id       |
| `width`          | string    | 否   | `default` | 选择器宽度                   |
| `disabled`       | boolean   | 否   | `false`   | 是否禁用                     |
| `confirm`        | object    | 否   | -           | 二次确认配置                 |
| `behaviors`      | array     | 否   | -           | 交互行为列表                 |

**可嵌套在**: Card Body, Column, Form

**JSON 示例**:

```json
{
  "tag": "select_person",
  "name": "assignee",
  "placeholder": {
    "tag": "plain_text",
    "content": "请选择人员"
  }
}
```

---

### 6.4 multi_select_person (人员选择-多选)

人员选择-多选组件支持添加指定人员作为多选选项。

| 字段                | 类型           | 必填 | 默认值      | 说明                                   |
| ------------------- | -------------- | ---- | ----------- | -------------------------------------- |
| `tag`             | string         | 是   | -           | 固定为 `"multi_select_person"`       |
| `element_id`      | string         | 否   | -           | 元素唯一标识                           |
| `name`            | string         | 否   | -           | 选择器唯一标识（表单中必填）           |
| `required`        | boolean        | 否   | `false`   | 是否必填                               |
| `placeholder`     | PlainText      | 否   | -           | 占位文本                               |
| `selected_values` | string[]       | 否   | -           | 默认选中用户的 open_id 列表            |
| `options`         | PersonOption[] | 否   | -           | 候选人员列表（为空时展示会话所有成员） |
| `width`           | string         | 否   | `default` | 选择器宽度                             |
| `disabled`        | boolean        | 否   | `false`   | 是否禁用                               |
| `confirm`         | object         | 否   | -           | 二次确认配置                           |
| `behaviors`       | array          | 否   | -           | 交互行为列表                           |

**PersonOption (人员选项) 结构**:

| 字段      | 类型   | 必填 | 说明           |
| --------- | ------ | ---- | -------------- |
| `value` | string | 是   | 用户的 open_id |

**可嵌套在**: Card Body, Column, Form

**JSON 示例**:

```json
{
  "tag": "multi_select_person",
  "name": "reviewers",
  "placeholder": {
    "tag": "plain_text",
    "content": "请选择审批人"
  },
  "options": [
    {
      "value": "ou_xxxxxxxx"
    },
    {
      "value": "ou_yyyyyyyy"
    }
  ]
}
```

---

### 6.5 Checker (勾选器)

勾选器是一种交互组件，支持配置回调响应，主要用于任务勾选的场景。

| 字段                  | 类型      | 必填 | 默认值    | 说明                                    |
| --------------------- | --------- | ---- | --------- | --------------------------------------- |
| `tag`               | string    | 是   | -         | 固定为 `"checker"`                    |
| `element_id`        | string    | 否   | -         | 元素唯一标识                            |
| `name`              | string    | 否   | -         | 勾选器唯一标识（表单中必填）            |
| `checked`           | boolean   | 否   | `false` | 初始勾选状态                            |
| `text`              | Element   | 否   | -         | 勾选器文本内容（PlainText 或 Markdown） |
| `overall_checkable` | boolean   | 否   | `true`  | 光标悬浮时是否有阴影效果                |
| `button_area`       | object    | 否   | -         | 按钮区配置                              |
| `checked_style`     | object    | 否   | -         | 勾选状态样式                            |
| `margin`            | string    | 否   | `0px`   | 外边距                                  |
| `padding`           | string    | 否   | `0px`   | 内边距                                  |
| `confirm`           | object    | 否   | -         | 二次确认配置                            |
| `behaviors`         | array     | 否   | -         | 交互行为列表                            |
| `hover_tips`        | PlainText | 否   | -         | 悬浮提示                                |
| `disabled`          | boolean   | 否   | `false` | 是否禁用                                |
| `disabled_tips`     | PlainText | 否   | -         | 禁用提示                                |

**checked_style (勾选状态样式) 结构**:

| 字段                   | 类型    | 必填 | 默认值    | 说明                     |
| ---------------------- | ------- | ---- | --------- | ------------------------ |
| `show_strikethrough` | boolean | 否   | `false` | 是否展示删除线           |
| `opacity`            | number  | 否   | `1`     | 不透明度，取值范围 [0,1] |

**可嵌套在**: Card Body, Column, Form, CollapsiblePanel

**JSON 示例**:

```json
{
  "tag": "checker",
  "name": "task_1",
  "checked": false,
  "text": {
    "tag": "lark_md",
    "content": "完成任务 1"
  },
  "checked_style": {
    "show_strikethrough": true,
    "opacity": 0.5
  },
  "behaviors": [
    {
      "type": "callback",
      "value": {
        "task_id": "task_1"
      }
    }
  ]
}
```

---

## 7. 媒体组件

### 7.1 Image (图片)

图片组件支持通过调用上传图片接口或在搭建工具中上传图片，获取图片的 key 传入图片组件中。

| 字段              | 类型      | 必填 | 默认值          | 说明                                                     |
| ----------------- | --------- | ---- | --------------- | -------------------------------------------------------- |
| `tag`           | string    | 是   | -               | 固定为 `"img"`                                         |
| `element_id`    | string    | 否   | -               | 元素唯一标识                                             |
| `img_key`       | string    | 是   | -               | 图片资源的 Key                                           |
| `alt`           | PlainText | 是   | -               | 悬浮在图片上时展示的说明文案                             |
| `title`         | PlainText | 否   | -               | 图片标题                                                 |
| `scale_type`    | string    | 否   | `crop_center` | 图片裁剪模式                                             |
| `size`          | string    | 否   | -               | 图片尺寸（scale_type 为 crop_center 或 crop_top 时生效） |
| `corner_radius` | string    | 否   | -               | 图片圆角半径                                             |
| `transparent`   | boolean   | 否   | `false`       | 是否为透明底色                                           |
| `preview`       | boolean   | 否   | `true`        | 点击后是否放大图片                                       |
| `margin`        | string    | 否   | `0px`         | 外边距                                                   |

**scale_type (裁剪模式) 枚举值**:

| 值                 | 说明           |
| ------------------ | -------------- |
| `crop_center`    | 居中裁剪       |
| `crop_top`       | 顶部裁剪       |
| `fit_horizontal` | 完整展示不裁剪 |

**size (尺寸) 枚举值**:

| 值                        | 说明                                 |
| ------------------------- | ------------------------------------ |
| `stretch`               | 超大图，适用于高宽比小于 16:9 的图片 |
| `large`                 | 大图，尺寸为 160×160                |
| `medium`                | 中图，尺寸为 80×80                  |
| `small`                 | 小图，尺寸为 40×40                  |
| `tiny`                  | 超小图，尺寸为 16×16                |
| `[1,1000]px [1,1000]px` | 自定义图片尺寸                       |

**注意事项**:

- 图片尺寸建议在 1500×3000 px 范围内
- 图片大小不超过 10M
- 图片高度:宽度不超过 16:9
- JSON 2.0 中不再支持 `size` 属性传入 `stretch_without_padding`，需设置 `margin` 为负数实现通栏效果

**可嵌套在**: Card Body, Div, Column, Form

**JSON 示例**:

```json
{
  "tag": "img",
  "img_key": "img_v3_0238_073f1823-df2b-4377-86c6-e293f183622j",
  "scale_type": "crop_center",
  "size": "stretch",
  "alt": {
    "tag": "plain_text",
    "content": "示例图片"
  },
  "corner_radius": "5px",
  "preview": true
}
```

---

### 7.2 ImgCombination (多图混排)

多图混排组件支持在卡片内添加多张图片，支持双图混排、三图混排、四宫格、六宫格、九宫格等多种混排方式。

| 字段                        | 类型      | 必填 | 默认值    | 说明                         |
| --------------------------- | --------- | ---- | --------- | ---------------------------- |
| `tag`                     | string    | 是   | -         | 固定为 `"img_combination"` |
| `element_id`              | string    | 否   | -         | 元素唯一标识                 |
| `combination_mode`        | string    | 是   | -         | 多图混排方式                 |
| `combination_transparent` | boolean   | 否   | `false` | 是否为透明底色               |
| `corner_radius`           | string    | 否   | -         | 图片圆角半径                 |
| `img_list`                | ImgItem[] | 是   | -         | 图片资源数组                 |
| `margin`                  | string    | 否   | `0px`   | 外边距                       |

**combination_mode (混排方式) 枚举值**:

| 值            | 说明     |
| ------------- | -------- |
| `double`    | 双图混排 |
| `triple`    | 三图混排 |
| `quadruple` | 四宫格   |
| `grid_six`  | 六宫格   |
| `grid_nine` | 九宫格   |

**ImgItem (图片项) 结构**:

| 字段            | 类型    | 必填 | 说明           |
| --------------- | ------- | ---- | -------------- |
| `img_key`     | string  | 是   | 图片的 Key     |
| `transparent` | boolean | 否   | 是否为透明底色 |

**可嵌套在**: Card Body, Div, Column, Form

**JSON 示例**:

```json
{
  "tag": "img_combination",
  "combination_mode": "quadruple",
  "corner_radius": "12px",
  "img_list": [
    {
      "img_key": "img_v3_0239_8347760e-3173-4072-b1aa-e4e7c835741j"
    },
    {
      "img_key": "img_v3_0239_d9a9b734-57f8-4247-baf3-ae178b55f96j"
    },
    {
      "img_key": "img_v3_0239_8347760e-3173-4072-b1aa-e4e7c835742j"
    },
    {
      "img_key": "img_v3_0239_d9a9b734-57f8-4247-baf3-ae178b55f97j"
    }
  ]
}
```

---

## 8. 展示组件

### 8.1 Table (表格)

表格组件支持在表格中添加普通文本、选项标签、人员列表以及数字格式的内容。

| 字段                    | 类型          | 必填 | 默认值    | 说明                                     |
| ----------------------- | ------------- | ---- | --------- | ---------------------------------------- |
| `tag`                 | string        | 是   | -         | 固定为 `"table"`                       |
| `element_id`          | string        | 否   | -         | 元素唯一标识                             |
| `page_size`           | int           | 否   | `5`     | 每页最大展示的数据行数，支持 [1,10] 整数 |
| `row_height`          | string        | 否   | `low`   | 行高设置                                 |
| `row_max_height`      | string        | 否   | `124px` | 最大行高（row_height 为 auto 时生效）    |
| `freeze_first_column` | boolean       | 否   | `false` | 是否冻结首列                             |
| `header_style`        | object        | 否   | -         | 表头样式                                 |
| `columns`             | TableColumn[] | 是   | -         | 列对象数组，最多 50 列                   |
| `rows`                | object[]      | 是   | -         | 行对象数组                               |
| `margin`              | string        | 否   | `0px`   | 外边距                                   |

**row_height (行高) 枚举值**:

| 值             | 说明       |
| -------------- | ---------- |
| `low`        | 低         |
| `middle`     | 中         |
| `high`       | 高         |
| `auto`       | 自适应内容 |
| `[32,124]px` | 自定义行高 |

**header_style (表头样式) 结构**:

| 字段                 | 类型    | 必填 | 默认值      | 说明         |
| -------------------- | ------- | ---- | ----------- | ------------ |
| `text_align`       | string  | 否   | `left`    | 文本对齐方式 |
| `text_size`        | string  | 否   | `normal`  | 字号         |
| `background_style` | string  | 否   | `none`    | 背景色       |
| `text_color`       | string  | 否   | `default` | 文本颜色     |
| `bold`             | boolean | 否   | `true`    | 是否加粗     |
| `lines`            | int     | 否   | `1`       | 文本行数     |

**TableColumn (列) 结构**:

| 字段                 | 类型   | 必填 | 默认值     | 说明                                       |
| -------------------- | ------ | ---- | ---------- | ------------------------------------------ |
| `name`             | string | 是   | -          | 列的 key，用于行数据中指定数据填充的单元格 |
| `display_name`     | string | 否   | -          | 列的展示名称                               |
| `width`            | string | 否   | `auto`   | 列宽度                                     |
| `data_type`        | string | 是   | `text`   | 列数据类型                                 |
| `vertical_align`   | string | 否   | `center` | 垂直对齐方式                               |
| `horizontal_align` | string | 否   | -          | 水平对齐方式                               |
| `format`           | object | 否   | -          | 数字格式配置（data_type 为 number 时）     |
| `date_format`      | string | 否   | -          | 日期格式（data_type 为 date 时）           |

**data_type (数据类型) 枚举值**:

| 值           | 说明                     |
| ------------ | ------------------------ |
| `text`     | 普通文本                 |
| `lark_md`  | 支持 Markdown 格式的文本 |
| `options`  | 选项标签                 |
| `number`   | 数字                     |
| `persons`  | 人员列表                 |
| `date`     | 日期时间                 |
| `markdown` | 完整 Markdown 语法       |

**注意事项**:

- 单张卡片最多支持放置五个表格组件
- 表格组件不可被内嵌在其它组件内，只可放在卡片根节点下

**JSON 示例**:

```json
{
  "tag": "table",
  "page_size": 5,
  "row_height": "low",
  "header_style": {
    "text_align": "left",
    "text_size": "normal",
    "background_style": "none",
    "text_color": "grey",
    "bold": true
  },
  "columns": [
    {
      "name": "customer_name",
      "display_name": "客户名称",
      "data_type": "text"
    },
    {
      "name": "customer_scale",
      "display_name": "客户规模",
      "data_type": "options"
    },
    {
      "name": "customer_arr",
      "display_name": "ARR(万元)",
      "data_type": "number",
      "format": {
        "symbol": "¥",
        "precision": 2,
        "separator": true
      }
    }
  ],
  "rows": [
    {
      "customer_name": "飞书科技",
      "customer_scale": [{"text": "S2", "color": "blue"}],
      "customer_arr": 168
    }
  ]
}
```

---

### 8.2 Hr (分割线)

分割线组件是一条长横线，用于分割卡片的内容，使呈现内容更清晰。

| 字段           | 类型   | 必填 | 默认值  | 说明            |
| -------------- | ------ | ---- | ------- | --------------- |
| `tag`        | string | 是   | -       | 固定为 `"hr"` |
| `element_id` | string | 否   | -       | 元素唯一标识    |
| `margin`     | string | 否   | `0px` | 外边距          |

**可嵌套在**: Card Body, Div, Column, Form

**JSON 示例**:

```json
{
  "tag": "hr"
}
```

---

### 8.3 Person (人员)

人员组件支持展示人员的用户名和头像。

| 字段            | 类型    | 必填 | 默认值     | 说明                             |
| --------------- | ------- | ---- | ---------- | -------------------------------- |
| `tag`         | string  | 是   | -          | 固定为 `"person"`              |
| `element_id`  | string  | 否   | -          | 元素唯一标识                     |
| `user_id`     | string  | 否   | -          | 用户的 user_id                   |
| `open_id`     | string  | 否   | -          | 用户的 open_id                   |
| `union_id`    | string  | 否   | -          | 用户的 union_id                  |
| `style`       | string  | 否   | `normal` | 展示样式：`normal`/`capsule` |
| `show_name`   | boolean | 否   | `true`   | 是否展示用户名                   |
| `show_avatar` | boolean | 否   | `true`   | 是否展示头像                     |
| `margin`      | string  | 否   | `0px`    | 外边距                           |

**可嵌套在**: Card Body, Div, Column, Form

**JSON 示例**:

```json
{
  "tag": "person",
  "open_id": "ou_c99c5f35d542efc7ee492afe11af19ef",
  "style": "normal",
  "show_name": true,
  "show_avatar": true
}
```

---

### 8.4 PersonList (人员列表)

人员列表组件支持展示多个人员的用户名和头像。

| 字段           | 类型     | 必填 | 默认值     | 说明                     |
| -------------- | -------- | ---- | ---------- | ------------------------ |
| `tag`        | string   | 是   | -          | 固定为 `"person_list"` |
| `element_id` | string   | 否   | -          | 元素唯一标识             |
| `user_ids`   | string[] | 否   | -          | 用户的 user_id 列表      |
| `open_ids`   | string[] | 否   | -          | 用户的 open_id 列表      |
| `union_ids`  | string[] | 否   | -          | 用户的 union_id 列表     |
| `style`      | string   | 否   | `normal` | 展示样式                 |
| `margin`     | string   | 否   | `0px`    | 外边距                   |

**可嵌套在**: Card Body, Div, Column, Form

**JSON 示例**:

```json
{
  "tag": "person_list",
  "open_ids": [
    "ou_c99c5f35d542efc7ee492afe11af19ef",
    "ou_d88d6g46e653fgd8ff603c6f22bg10fg"
  ]
}
```

---

### 8.5 Chart (图表)

图表组件基于 VChart 的图表定义，支持折线图、面积图、柱状图、饼图、词云等多种数据呈现方式。

| 字段           | 类型   | 必填 | 默认值   | 说明               |
| -------------- | ------ | ---- | -------- | ------------------ |
| `tag`        | string | 是   | -        | 固定为 `"chart"` |
| `element_id` | string | 否   | -        | 元素唯一标识       |
| `chart_type` | string | 是   | -        | 图表类型           |
| `data`       | object | 是   | -        | 图表数据           |
| `width`      | string | 否   | `fill` | 图表宽度           |
| `margin`     | string | 否   | `0px`  | 外边距             |

**可嵌套在**: Card Body, Div, Column, Form

**JSON 示例**:

```json
{
  "tag": "chart",
  "chart_type": "line",
  "data": {
    "labels": ["一月", "二月", "三月"],
    "datasets": [
      {
        "label": "销售额",
        "data": [100, 200, 150]
      }
    ]
  }
}
```

---

## 9. 交互行为类型

交互行为用于定义组件被点击或操作时的响应动作。在 JSON 2.0 结构中，通过 `behaviors` 数组配置交互行为。

### 9.1 CallbackBehavior (回调行为)

回调行为用于将数据回传到开发者配置的服务器。

| 字段      | 类型   | 必填 | 说明                       |
| --------- | ------ | ---- | -------------------------- |
| `type`  | string | 是   | 固定为 `"callback"`      |
| `value` | object | 是   | 回传数据，支持 object 类型 |

**JSON 示例**:

```json
{
  "type": "callback",
  "value": {
    "action": "submit",
    "data": {
      "id": 123
    }
  }
}
```

---

### 9.2 OpenUrlBehavior (打开 URL 行为)

打开 URL 行为用于点击后跳转到指定的链接地址。

| 字段            | 类型   | 必填 | 说明                  |
| --------------- | ------ | ---- | --------------------- |
| `type`        | string | 是   | 固定为 `"open_url"` |
| `default_url` | string | 是   | 兜底跳转地址          |
| `android_url` | string | 否   | 安卓端跳转地址        |
| `ios_url`     | string | 否   | iOS 端跳转地址        |
| `pc_url`      | string | 否   | PC 端跳转地址         |

**注意事项**:

- 超链接必须包含 schema 才能生效，目前仅支持 HTTP 和 HTTPS
- 如果需要禁止某端进行跳转，可以将对应的参数值配置为 `lark://msgcard/unsupported_action`

**JSON 示例**:

```json
{
  "type": "open_url",
  "default_url": "https://example.com",
  "pc_url": "https://example.com/pc",
  "ios_url": "lark://msgcard/unsupported_action",
  "android_url": "https://example.com/android"
}
```

---

### 9.3 FormSubmitBehavior (表单提交)

表单提交行为用于触发表单容器的提交事件，将表单数据提交到服务器。

| 字段     | 类型   | 必填 | 说明                     |
| -------- | ------ | ---- | ------------------------ |
| `type` | string | 是   | 固定为 `"form_submit"` |

**JSON 示例**:

```json
{
  "type": "form_submit"
}
```

---

### 9.4 FormResetBehavior (表单重置)

表单重置行为用于重置表单容器中所有表单项的值为初始值。

| 字段     | 类型   | 必填 | 说明                    |
| -------- | ------ | ---- | ----------------------- |
| `type` | string | 是   | 固定为 `"form_reset"` |

**JSON 示例**:

```json
{
  "type": "form_reset"
}
```

---

## 10. 二次确认弹窗

二次确认弹窗用于在用户提交时弹出确认提示，只有用户点击确认后，才执行相应的操作。

| 字段      | 类型      | 必填 | 说明         |
| --------- | --------- | ---- | ------------ |
| `title` | PlainText | 是   | 弹窗标题     |
| `text`  | PlainText | 是   | 弹窗正文内容 |

**注意事项**:

- `confirm` 字段仅在用户点击包含提交属性的按钮时才会触发二次确认弹窗
- 弹窗默认提供了确认和取消按钮

**JSON 示例**:

```json
{
  "confirm": {
    "title": {
      "tag": "plain_text",
      "content": "确认提交"
    },
    "text": {
      "tag": "plain_text",
      "content": "提交后将无法修改，是否确认提交？"
    }
  }
}
```

---

## 11. 组件嵌套规则

### 11.1 父组件可包含的子元素

| 父组件               | 可包含的子元素                                                                                               |
| -------------------- | ------------------------------------------------------------------------------------------------------------ |
| Card Body            | 所有元素类型                                                                                                 |
| Div                  | 所有元素类型                                                                                                 |
| Column               | 所有元素类型（除表单容器和表格外）                                                                           |
| Form                 | Input, Select, DatePicker, PickerTime, PickerDatetime, Checkbox, Button, Div, PlainText, Markdown, ColumnSet |
| ColumnSet            | Column                                                                                                       |
| InteractiveContainer | 所有元素类型（除表单容器和表格外）                                                                           |

### 11.2 元素可嵌套的父组件

| 元素类型       | 可嵌套在                          |
| -------------- | --------------------------------- |
| PlainText      | Card Body, Div, Column, Form      |
| Markdown       | Card Body, Div, Column, Form      |
| Div            | Card Body, Div, Column, Form      |
| Button         | Card Body, Div, Column, Form      |
| Input          | Card Body, Column, Form           |
| Select         | Card Body, Column, Form           |
| DatePicker     | Card Body, Column, Form           |
| PickerTime     | Card Body, Column, Form           |
| PickerDatetime | Card Body, Column, Form           |
| Checkbox       | Card Body, Column, Form           |
| Image          | Card Body, Div, Column, Form      |
| Table          | Card Body（不可嵌套在其他组件内） |

### 11.3 嵌套层级限制

- 容器类组件最多支持嵌套五层组件
- 建议避免多层嵌套，会压缩内容的展示空间，影响卡片的展示效果
  | Button | Card Body, Div, Column, Form |
  | Input | Card Body, Form |
  | Select | Card Body, Form |
  | DatePicker | Card Body, Form |
  | Checkbox | Card Body, Form |
  | Image | Card Body, Div, Column, Form |
  | ColumnSet | Card Body, Form |
  | Form | Card Body |

---

## 10. 完整 JSON 示例

### 10.1 简单文本卡片

```json
{
  "schema": "2.0",
  "header": {
    "title": {
      "tag": "plain_text",
      "content": "欢迎使用飞书卡片"
    }
  },
  "body": {
    "elements": [
      {
        "tag": "plain_text",
        "content": "这是卡片内容"
      }
    ]
  }
}
```

### 10.2 Markdown 内容卡片

```json
{
  "schema": "2.0",
  "header": {
    "title": {
      "tag": "plain_text",
      "content": "Markdown 示例"
    }
  },
  "body": {
    "elements": [
      {
        "tag": "markdown",
        "content": "**粗体文本**\n*斜体文本*\n\n- 列表项 1\n- 列表项 2\n- 列表项 3"
      }
    ]
  }
}
```

### 10.3 分栏布局卡片

```json
{
  "schema": "2.0",
  "header": {
    "title": {
      "tag": "plain_text",
      "content": "分栏布局示例"
    }
  },
  "body": {
    "elements": [
      {
        "tag": "column_set",
        "columns": [
          {
            "tag": "column",
            "width": "33%",
            "elements": [
              {
                "tag": "plain_text",
                "content": "第一列"
              }
            ]
          },
          {
            "tag": "column",
            "width": "33%",
            "elements": [
              {
                "tag": "plain_text",
                "content": "第二列"
              }
            ]
          },
          {
            "tag": "column",
            "width": "34%",
            "elements": [
              {
                "tag": "plain_text",
                "content": "第三列"
              }
            ]
          }
        ]
      }
    ]
  }
}
```

### 10.4 表单卡片

```json
{
  "schema": "2.0",
  "header": {
    "title": {
      "tag": "plain_text",
      "content": "用户信息收集"
    }
  },
  "body": {
    "elements": [
      {
        "tag": "form",
        "name": "userForm",
        "direction": "vertical",
        "elements": [
          {
            "tag": "input",
            "name": "username",
            "label": {
              "tag": "plain_text",
              "content": "姓名"
            },
            "placeholder": {
              "tag": "plain_text",
              "content": "请输入姓名"
            },
            "required": true
          },
          {
            "tag": "select",
            "name": "gender",
            "label": {
              "tag": "plain_text",
              "content": "性别"
            },
            "placeholder": {
              "tag": "plain_text",
              "content": "请选择"
            },
            "options": [
              {
                "value": "male",
                "text": {
                  "tag": "plain_text",
                  "content": "男"
                }
              },
              {
                "value": "female",
                "text": {
                  "tag": "plain_text",
                  "content": "女"
                }
              }
            ]
          },
          {
            "tag": "date_picker",
            "name": "birthday",
            "placeholder": {
              "tag": "plain_text",
              "content": "请选择日期"
            },
            "initial_date": "2000-01-01"
          },
          {
            "tag": "button",
            "text": {
              "tag": "plain_text",
              "content": "提交"
            },
            "type": "primary",
            "behaviors": [
              {
                "type": "form_submit"
              }
            ]
          }
        ]
      }
    ]
  }
}
```

### 10.5 复杂布局卡片

```json
{
  "schema": "2.0",
  "config": {
    "update_multi": true
  },
  "header": {
    "title": {
      "tag": "plain_text",
      "content": "产品发布公告"
    },
    "padding": "12px"
  },
  "body": {
    "elements": [
      {
        "tag": "img",
        "img_key": "img_banner123",
        "size": "crop_center",
        "width": "100%"
      },
      {
        "tag": "div",
        "text": {
          "tag": "markdown",
          "content": "**新产品上线！**\n\n我们很高兴地宣布新产品正式发布。"
        }
      },
      {
        "tag": "column_set",
        "horizontal_spacing": "12px",
        "columns": [
          {
            "tag": "column",
            "width": "50%",
            "elements": [
              {
                "tag": "markdown",
                "content": "**功能特点**\n- 特性 1\n- 特性 2"
              }
            ]
          },
          {
            "tag": "column",
            "width": "50%",
            "elements": [
              {
                "tag": "markdown",
                "content": "**适用场景**\n- 场景 1\n- 场景 2"
              }
            ]
          }
        ]
      },
      {
        "tag": "div",
        "horizontal_align": "center",
        "elements": [
          {
            "tag": "button",
            "text": {
              "tag": "plain_text",
              "content": "了解更多"
            },
            "type": "primary",
            "behaviors": [
              {
                "type": "open_url",
                "default_url": "https://example.com/product"
              }
            ]
          }
        ]
      }
    ]
  }
}
```

---

## 附录

### A. 常用颜色值

| 颜色名称        | 十六进制 |
| --------------- | -------- |
| 蓝色（Primary） | #3370FF  |
| 红色（Danger）  | #F53F3F  |
| 绿色（Success） | #00B365  |
| 橙色（Warning） | #FF7D00  |
| 灰色（Default） | #86909C  |

### B. 常用间距值

| 间距名称    | 值   |
| ----------- | ---- |
| Small       | 4px  |
| Medium      | 8px  |
| Large       | 12px |
| Extra Large | 16px |

### C. 错误码参考

| 错误码 | 说明               |
| ------ | ------------------ |
| 230099 | 卡片内容解析失败   |
| 200621 | 卡片 JSON 格式错误 |
| 200861 | 不支持的组件或属性 |

---

*文档版本：1.0*
*最后更新：2026-03-07*
